# Meta
Include "sites-available/meta.wikimedia.org.conf"
# Wikisource
Include "sites-available/_wikisource.org.conf"
# Wikimedia Commons
Include "sites-available/commons.wikimedia.org.conf"

# Internal grants wiki
Include "sites-available/grants.wikimedia.org.conf"

# fdc.wikimedia.org - Funds Dissemination Committee Wik
Include "sites-available/fdc.wikimedia.org.conf"

# Internal wiki
Include "sites-available/internal.wikimedia.org.conf"


# Board wiki
Include "sites-available/board.wikimedia.org.conf"

# Board Governance Committee
Include "sites-available/boardgovcom.wikimedia.org.conf"

# Spcom wiki
Include "sites-available/spcom.wikimedia.org.conf"

# Affcom wiki
Include "sites-available/affcom.wikimedia.org.conf"

# Incubator wiki
<VirtualHost *:80>
    ServerName incubator.wikimedia.org

    RewriteEngine On
    RewriteRule . - [E=RW_PROTO:%{HTTP:X-Forwarded-Proto}]
    RewriteCond %{ENV:RW_PROTO} !=https
    RewriteRule . - [E=RW_PROTO:http]

    Include "sites-enabled/wikimedia-common.incl"
    Include "sites-enabled/wikimedia-legacy.incl"
</VirtualHost>

# WikiSpecies wiki
<VirtualHost *:80>
    ServerName species.wikimedia.org

    RewriteEngine On
    RewriteRule . - [E=RW_PROTO:%{HTTP:X-Forwarded-Proto}]
    RewriteCond %{ENV:RW_PROTO} !=https
    RewriteRule . - [E=RW_PROTO:http]

    Include "sites-enabled/wikimedia-common.incl"
    Include "sites-enabled/wikimedia-legacy.incl"
    # Uploads are offsite
    RewriteRule ^/upload/(.*)$ %{ENV:RW_PROTO}://upload.wikimedia.org/wikipedia/species/$1 [R=302]
</VirtualHost>

# search committee private wiki
Include "sites-available/searchcom.wikimedia.org.conf"

# Usability Wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/wikimedia.org"
    ServerName usability.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteRule . - [E=RW_PROTO:%{HTTP:X-Forwarded-Proto}]
    RewriteCond %{ENV:RW_PROTO} !=https
    RewriteRule . - [E=RW_PROTO:http]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/wikimedia.org/w/index.php

    Include "sites-enabled/public-wiki-rewrites.incl"
    Include "sites-enabled/wikimedia-legacy.incl"

    RewriteRule ^/math/(.*) %{ENV:RW_PROTO}://upload.wikimedia.org/math/$1 [R=301]

    <Directory "/srv/mediawiki/docroot/wikimedia.org/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/wikimedia.org/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# strategy  wiki
<VirtualHost *:80>
    ServerName strategy.wikimedia.org

    RewriteEngine On
    RewriteRule . - [E=RW_PROTO:%{HTTP:X-Forwarded-Proto}]
    RewriteCond %{ENV:RW_PROTO} !=https
    RewriteRule . - [E=RW_PROTO:http]

    Include "sites-enabled/wikimedia-common.incl"
    Include "sites-enabled/wikimedia-legacy.incl"
</VirtualHost>

# office private wiki
Include "sites-available/office.wikimedia.org.conf"

# chair private wiki
Include "sites-available/chair.wikimedia.org.conf"

# advisory board PUBLIC wiki
<VirtualHost *:80>
    ServerName advisory.wikimedia.org

    RewriteEngine On
    Include "sites-enabled/wikimedia-common.incl"
    Include "sites-enabled/wikimedia-legacy.incl"
</VirtualHost>

# auditcom private wiki
Include "sites-available/auditcom.wikimedia.org.conf"

# quality private wiki
<VirtualHost *:80>
    ServerName quality.wikimedia.org

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://quality.wikimedia.org/$1 [R=301,L,NE]
    Include "sites-enabled/wikimedia-common.incl"
    Include "sites-enabled/wikimedia-legacy.incl"
</VirtualHost>

# OTRS private wiki
Include "sites-available/otrs-wiki.wikimedia.org.conf"

# Executive director's private wiki
Include "sites-available/exec.wikimedia.org.conf"

# Private wiki for Jay and his minions
Include "sites-available/collab.wikimedia.org.conf"

# Outreach Wiki
<VirtualHost *:80>
    ServerName outreach.wikimedia.org
    Include "sites-enabled/wikimedia-common.incl"
    Include "sites-enabled/wikimedia-legacy.incl"
</VirtualHost>

#movementroles wiki
Include "sites-available/movementroles.wikimedia.org.conf"

# checkuser private wiki
Include "sites-available/checkuser.wikimedia.org.conf"

# steward private wiki
Include "sites-available/steward.wikimedia.org.conf"

# ombudsmen private wiki
Include "sites-available/ombudsmen.wikimedia.org.conf"

# Project Grants Committee (private)
Include "sites-available/projectcom.wikimedia.org.conf"

# CoC Committee (private)
Include "sites-available/techconduct.wikimedia.org.conf"

# Election Committee (private)
Include "sites-available/electcom.wikimedia.org.conf"

# Advisors Wiki (private)
Include "sites-available/advisors.wikimedia.org.conf"

## donatewiki has been moved to main.conf so it can catch donate.wikipedia.org
