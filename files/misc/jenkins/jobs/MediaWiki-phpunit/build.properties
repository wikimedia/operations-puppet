builddir=${basedir}/workspace
sourcedir=${basedir}/workspace/mw-core
extensionsdir=${basedir}/workspace/mw-extensions
svn.up.args=--ignore-externals

# This is the part of the source checkout we're going to do things like PHPDoc, etc
sourcepart=.

javax.xml.transform.TransformerFactory=net.sf.saxon.TransformerFactoryImpl
