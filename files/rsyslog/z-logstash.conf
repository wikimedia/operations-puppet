# *** Pleae DO NOT change the file name, let it start with "z" to ensure
# it appears at the end when rsyslog does a listing of config files
# 
# This is used to make rsyslog as an agent for logstash server
# Project Name: Syslog Collection

$ModLoad imfile	#Loads the imfile input module to pull log from files

# We are just directing the logs to the log server
# rsyslog still writes logs locally

# Discards auth related logs, rest are sent to the server
auth,authpriv.*		~
*.*	@i-000003a9.pmtpa.wmflabs:5544
