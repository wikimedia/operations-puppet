filter {
  if [type] == "bash" {
    grok {
      match => [ "message", "^!bash %{GREEDYDATA:message}$" ]
      overwrite => [ "message" ]
      named_captures_only => true
    }
  } # end [type] == "bash"
}
