filter {
  if [type] == "irc" {
    mutate {
      add_tag => [ "es" ]
    }

    if [message] =~ /^!log / {
      clone {
        clones => [ "sal" ]
        remove_tag => [ "es" ]
      }
    } # end [message] =~ /^!log /
  } # end [type] == "irc"
}
