profile::elasticsearch::common_settings:
  disktype: 'ssd'
  heap_memory: '32G'

profile::opensearch::server::common_settings:
  disktype: 'ssd'
  heap_memory: '32G'

lookup_options:
  profile::elasticsearch::common_settings:
    merge:
      strategy: deep
  profile::opensearch::server::common_settings:
    merge:
      strategy: deep
