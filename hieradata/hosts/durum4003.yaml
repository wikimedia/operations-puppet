# this durum host runs on Routed Ganeti,
# it should peer with the hypervisor and not the core routers
# To be removed once ulsfo is on the new design
# And profile::bird::neighbors_list is removed from the site
# Or moved to the ulsfo durum role after the durum hosts are migrated to routed ganeti
profile::bird::neighbors_list: ~