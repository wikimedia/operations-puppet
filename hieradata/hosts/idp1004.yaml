# SPDX-License-Identifier: Apache-2.0
profile::idp::tomcat_version: 'tomcat10'
profile::java::hardened_tls: false
profile::java::java_packages:
  - version: "21"
    variant: "jdk"

# TODO: Add idp2004 once it exists.
# Puppet will fail to run if added before the
# hosts is created.
profile::idp::idp_nodes:
  - idp1004.wikimedia.org

