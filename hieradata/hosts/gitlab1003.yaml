# service IP for public https and ssh (T285870)
profile::gitlab::service_ip_v4: '208.80.154.15'
profile::gitlab::service_ip_v6: '2620:0:861:1:208:80:154:15'
profile::gitlab::service_name: 'gitlab-replica-a.wikimedia.org'
profile::gitlab::ssh_extra_host_aliases:
  - 'gitlab-ssh-replica-a.wikimedia.org'
  - 'gitlab-ssh.wikimedia.org'
profile::gitlab::gitlab_ssh_host: 'gitlab-ssh-replica-a.wikimedia.org'

# nftables throttling disabled on replicas, still enabled on the active host
# via role/common/gitlab.yaml. Flip to 'present' to re-enable. The prometheus
# denylist exporter follows this value automatically.
profile::firewall::nftables_throttling::ensure: absent