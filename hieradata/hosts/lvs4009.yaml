profile::liberica::bgp_config:
  communities:
    - "14907:0" # lowest priority to avoid interfering with traffic while reimaging
profile::liberica::include_services:
  - upload
  - upload-https
# keep bullseye device name of the NIC to avoid blocking the reimage
profile::lvs::interface_tweaks:
  enp175s0f0np0:
    txqlen: 10000
