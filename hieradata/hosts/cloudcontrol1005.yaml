profile::openstack::eqiad1::galera::server_id: 3

# TODO: drop this override when the migration is completed
profile::openstack::eqiad1::haproxy_nodes:
  - cloudlb1001.private.eqiad.wikimedia.cloud
  - cloudlb1002.private.eqiad.wikimedia.cloud
  - cloudcontrol1006.wikimedia.org
  - cloudcontrol1007.wikimedia.org

# TODO: we can't set this as default because this key also configures
# DNS stuff which needs the .wikimedia.org public IPv4 addresses (for now)
# However cloudservices <-> cloudcontrol network flows needs to be opened
# for the cloud-private addresses, so that's why this override here.
profile::openstack::eqiad1::designate_hosts:
  - cloudservices1006.private.eqiad.wikimedia.cloud

# TODO: move to hieradata/role/eqiad/wmcs/openstack/eqiad1/control.yaml
# when the migration is completed
profile::memcached::srange: "%{lookup('profile::wmcs::cloud_private_subnet::supernet')}"
