# T428495: Explicitly enable the v2 API.
profile::etcd::v3::enable_v2: true

# T428495: Explicitly set cluster_bootstrap to false.
profile::etcd::v3::cluster_bootstrap: false

# T428495: Temporarily enable etcd-mirror replication (switch from conf1008).
profile::etcd::replication::active: true
