profile::openstack::base::pdns::query_local_address: ns0.openstack.codfw1dev.wikimediacloud.org

profile::openstack::base::pdns::auth::listen_on:
  - 185.15.57.25  # ns0.openstack.codfw1dev.wikimediacloud.org

profile::openstack::base::pdns::pdns_webserver_address: 172.20.5.8

profile::bird::advertise_vips:
  # NOTE: this is the per-host address. There is another shared VIP.
  ns0.openstack.codfw1dev.wikimediacloud.org:
    ensure: present
    service_type: "pdns auth"
    address: 185.15.57.25
    # NOTE: we always want to advertise this address
    check_cmd: "/bin/sh -c 'true'"

profile::openldap::master: cloudservices2005-dev.codfw.wmnet
profile::openldap::server_id: 3

profile::openstack::codfw1dev::version: 'zed'
