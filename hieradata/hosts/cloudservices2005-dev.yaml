profile::openstack::base::pdns::query_local_address: ns1.openstack.codfw1dev.wikimediacloud.org

profile::openstack::base::pdns::auth::listen_on:
  - 185.15.57.26  # ns1.openstack.codfw1dev.wikimediacloud.org

profile::bird::advertise_vips:
  # NOTE: this is the per-host address. There is another shared VIP.
  ns1.openstack.codfw1dev.wikimediacloud.org:
    ensure: present
    service_type: "pdns auth"
    # TODO: verify address
    address: 185.15.57.26
    # NOTE: we always want to advertise this address
    check_cmd: "/bin/sh -c 'true'"


profile::openldap::master: cloudservices2004-dev.codfw.wmnet
profile::openldap::server_id: 4

profile::openstack::codfw1dev::version: 'zed'
