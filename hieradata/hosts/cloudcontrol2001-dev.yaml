labsldapconfig:
  hostname: cloudservices2002-dev.wikimedia.org
  secondary_hostname: cloudservices2002-dev.wikimedia.org

profile::openstack::codfw1dev::fernet_key_rotate_time: '00:00:00'
profile::openstack::codfw1dev::fernet_key_sync_time: '01:00:00'

profile::openstack::codfw1dev::galera::server_id: 1
profile::backup::enable: true

profile::wmcs::backy2::backup_time: '*-*-* 1:00:00'

profile::wmcs::backy2::backup_glance_images::enabled: true

# Temporary override; once everything is on bullseye this can be
#  set profile-wide
profile::openstack::codfw1dev::keystone::wsgi_server: 'keystone'
