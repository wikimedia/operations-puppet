# Temporarily disable the enwiki dumps on snapshot1012 - See #T368098
profile::dumps::generation::worker::dumper::ensure: absent
profile::dumps::generation::worker::common::use_analytics_replicas: true
profile::dumps::generation::worker::dumper::runtype: enwiki