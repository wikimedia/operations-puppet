profile::cumin::master::insetup_role_report_day: 15  # 15th day of the month
profile::homer::private_git_peer: cumin1002.eqiad.wmnet
profile::homer::diff_timer_interval: '*-*-* 20:35:00'  # Avoid the first 10 minutes of the hour, to not overlap with rancid
profile::httpbb::test_kubernetes_hourly: true
profile::httpbb::hourly_tests:
  appserver: []
