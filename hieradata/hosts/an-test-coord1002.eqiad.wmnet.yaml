# Disable notifications while this host is being put into production. See #T406746
profile::monitoring::notifications_enabled: false

# Disable the systemd timers that are still running on an-test-coord1001.
profile::analytics::refinery::job::test::refine::ensure_timers: absent
profile::analytics::refinery::job::test::refine_sanitize::ensure_timers: absent
profile::analytics::refinery::job::test::data_purge::ensure_timers: absent