# service IP for public https and ssh (T285870)
profile::gitlab::service_ip_v4: '208.80.153.105'
profile::gitlab::service_ip_v6: '2620:0:860:4:208:80:153:105'
profile::gitlab::service_name: 'gitlab-replica.wikimedia.org'

# Enable Restore-Backup Timer Job
profile::gitlab::enable_restore: true

# backups saved on dedicated disk on ganti hosts
profile::gitlab::backup_dir_data: '/mnt/gitlab-backup'
