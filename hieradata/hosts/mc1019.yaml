# Specific settings for T208844
# Will be moved to role level once applied
# to all the shards.
profile::memcached::max_seq_reqs: 200

# Specific settings for T217731
# Performance test to see how evictions/hit-ratio
# changes adding more memory dedicated to memcached.
profile::memcached::size: '102400'
profile::base::puppet::puppet_major_version: 5
profile::base::puppet::facter_major_version: 3
