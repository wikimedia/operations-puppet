profile::elasticsearch::common_settings:
  disktype: 'ssd'
  heap_memory: '32G'

profile::opensearch::common_settings:
  disktype: 'ssd'
  heap_memory: '32G'

lookup_options:
  profile::elasticsearch::common_settings:
    merge:
      strategy: deep
  profile::opensearch::common_settings:
    merge:
      strategy: deep
