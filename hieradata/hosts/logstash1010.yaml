profile::elasticsearch::common_settings:
  heap_memory: '64G'

profile::opensearch::server::common_settings:
  heap_memory: '64G'

lookup_options:
  profile::elasticsearch::common_settings:
    merge:
      strategy: deep
  profile::opensearch::server::common_settings:
    merge:
      strategy: deep
