# == contint1001 ==
#
# hotspare is contint2001
jenkins::service_ensure: unmanaged
jenkins::service_enable: false
role::ci::master::jenkins_experimental: false

zuul::server::service_ensure: running
zuul::server::service_enable: true
