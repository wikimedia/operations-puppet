# This is the conftool cluster name, don't be confused
cluster: ml_serve

profile::admin::groups:
  - ml-team-admins
profile::contacts::role_contacts: ['Machine Learning']

profile::base::production::role_description: 'Machine Learning GPU host in setup.'

profile::amd_gpu::is_basic_gpu_node: true
