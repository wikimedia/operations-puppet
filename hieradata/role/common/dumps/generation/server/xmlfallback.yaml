cluster: dumps
profile::admin::groups:
  - dumps-roots

profile::contacts::role_contacts: ['Data Platform']
profile::base::production::role_description: 'Fallback NFS server of xml dumps generation filesystem to dumps producer hosts'

profile::puppet::agent::force_puppet7: true
