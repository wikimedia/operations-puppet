profile::resolving::domain_search:
  - wikimedia.org
  - eqiad.wmnet
  - codfw.wmnet
cluster: misc
# Needed for docker
profile::base::overlayfs: true
profile::admin::groups:
  - restricted
  - deployment
  - deploy-ml-service
  - wdqs-admins
  - airflow-search-admins
  - deploy-design
  - deploy-phabricator
  - analytics-deployers
  - gerrit-deployers
  - zuul-deployers
  - deployment-ci-admins
  - deployment-jenkins
  - deployment-docker
  - research-deployers
  - platform-eng-deployers
  - airflow-analytics-product-admins
  - airflow-wmde-admins
  - release-engineering
  - airflow-deployers

# We still don't enable fpm here
profile::mediawiki::php::enable_fpm: false
profile::mediawiki::php::php_versions: ['8.3']
profile::mediawiki::php::absented_php_versions: ['8.1']

# Default scap server configuration.  This is used in production.
# If you are setting up a scap server in labs, these will be used
# unless you override them for your labs project.
# See the overrides in hieradata/labs/deployment-prep/common.yaml
# for an example.

# keyholder::agent declarations.  These are created
# by the profile::keyholder::server class.  Each agent listed here
# will be present and useable by scap on the scap deploy server.
# NOTE: since labs
profile::keyholder::server::agents:
  mwdeploy:
    trusted_groups:
      - wikidev
      - mwdeploy
      - mwbuilder
      - deployment

  phabricator:
    trusted_groups:
      - deploy-phabricator

  gerrit:
    trusted_groups:
      - gerrit-deployers

  deploy-service:
    trusted_groups:
      - deployment

  dumpsdeploy:
    trusted_groups:
      - ops

  analytics_deploy:
    trusted_groups:
      - analytics-deployers

  deploy-librenms:
    trusted_groups:
      - ops

  deploy-debmonitor:
    trusted_groups:
      - ops

  apache2modsec:
    trusted_groups:
      - ops
      - gerrit-admin

  netbox:
    trusted_groups:
      - ops

  deploy-homer:
    trusted_groups:
      - ops

  deploy-hiddenparma:
    trusted_groups:
      - ops

  deploy-ci-docroot:
    trusted_groups:
      - deployment-ci-admins

  deploy-zuul:
    trusted_groups:
      - zuul-deployers

  deploy-jenkins:
    trusted_groups:
      - deployment-jenkins

  deploy-design:
    trusted_groups:
      - deploy-design

  scap:
    trusted_groups:
      - deployment

  # Shared ssh key for Release Engineering's Train-related
  # Gerrit patches (T310620)
  trainbranchbot:
    trusted_groups:
      - deployment

profile::mediawiki::scap_client::is_master: true
# T329857
profile::mediawiki::scap_client::deployment_dir_linked_to_staging_dir: true

# scap::source declarations.  These are created
# by the profile::mediawiki::deployment::server class.
# Each source listed here will be cloned on the scap deploy server.
scap::sources:

  analytics/refinery:
    scap_repository: analytics/refinery/scap
  analytics/hdfs-tools/deploy: {}
  dumps/dumps:
    repository: operations/dumps
    scap_repository: operations/dumps/scap
  # Public EventStreams service
  eventstreams/deploy:
    repository: mediawiki/services/eventstreams/deploy
  gerrit/gerrit:
    repository: operations/software/gerrit
  gervert/deploy:
    repository: operations/software/gerrit/tools/gervert/deploy
  integration/docroot: {}
  logstash/plugins:
    repository: operations/software/logstash/plugins
  analytics/turnilo/deploy: {}
  phabricator/deployment:
    repository: phabricator/deployment
    origin: gitlab
  restbase/deploy:
    repository: mediawiki/services/restbase/deploy
  horizon/deploy:
    repository: openstack/horizon/deploy
  wikimedia/discovery/analytics:
    repository: wikimedia/discovery/analytics
  wdqs/wdqs:
    repository: wikidata/query/deploy
  #    lvs_service: wdqs
  # Cassandra logback encoder for Logstash
  cassandra/logstash-logback-encoder:
    repository: operations/software/logstash-logback-encoder
  hiddenparma/deploy:
    repository: operations/software/hiddenparma/deploy
  homer/deploy:
    repository: operations/software/homer/deploy
  zuul/deploy:
    repository: integration/zuul/deploy
  # Librenms software
  librenms/librenms:
    repository: operations/software/librenms
  # Netbox software
  netbox/deploy:
    repository: operations/software/netbox-deploy
  # Netbox software (dev)
  netbox-dev/deploy:
    repository: operations/software/netbox-deploy
  search/mjolnir/deploy:
    repository: search-platform/mjolnir-deploy
    origin: gitlab
  statsv/statsv:
    repository: performance/statsv
  performance/arc-lamp: {}
  performance/asoranking: {}
  performance/navtiming: {}
  docker-pkg/deploy:
    repository: operations/docker-images/docker-pkg/deploy
  debmonitor/deploy:
    repository: operations/software/debmonitor/deploy
  releng/phatality: {}
  design/style-guide: {}
  releng/jenkins-deploy:
    repository: releng/jenkins-deploy
    origin: gitlab

# Options for mw-on-k8s deployments for scap
scap::k8s_deployments:
  clusters:
    - eqiad
    - codfw
  file: /etc/helmfile-defaults/mediawiki-deployments.yaml
  releases_dir: /etc/helmfile-defaults/mediawiki/release

# List of kubernetes deployments of MediaWiki.
profile::kubernetes::deployment_server::mediawiki::release::mw_releases:
  - namespace: mw-debug
    releases:
      pinkunicorn:
        stage: testservers
      next:
        stage: testservers
    mw_kind: "debug-image"
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mw-experimental
    releases:
      pinkllama:
        deploy: false
    mw_kind: "debug-image"
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mw-web
    releases:
      main: {}
      canary:
        stage: "canaries"
      next: {}
      migration: {}
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mw-api-int
    releases:
      main: {}
      canary:
        stage: "canaries"
      migration: {}
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mw-api-ext
    releases:
      main: {}
      canary:
        stage: "canaries"
      next: {}
      migration: {}
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mw-jobrunner
    releases:
      main: {}
      canary:
        stage: "canaries"
      migration: {}
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mw-parsoid
    releases:
      pinkllama:
        deploy: false
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mw-misc
    releases:
      main:
        stage: testservers
    mw_kind: "debug-image"
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mw-wikifunctions
    releases:
      group0: {}
      group1: {}
      group2: {}
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mw-videoscaler
    releases:
      main: {}
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mw-cron
    releases:
      main: {}
    mw_kind: "cli-image"
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mw-script
    releases:
      main:
        deploy: false
      next:
        deploy: false
    mw_kind: "cli-image"
    mw_flavour: "publish-83"
    web_flavour: "webserver-bookworm"
  - namespace: mediawiki-dumps-legacy
    releases:
      production: {}
    mw_flavour: "publish-83"
    mw_kind: "cli-image"
    dir: "dse-k8s-services"
    clusters: ["dse-k8s-eqiad"]
    web_flavour: "webserver-bookworm"
profile::kubernetes::deployment_server::include_admin: true
# Default and environment specific generic values go here.
# default:
#   ..
# eqiad:
#   ..
profile::kubernetes::deployment_server::general:
  default:
    # Use the internal discovery domain as default registry
    docker:
      registry: "docker-registry.discovery.wmnet"
    # envoy
    mesh:
      image_version: 1.35.7-1
      telemetry:
        enabled: true
    # prometheus-statsd-exporter
    monitoring:
      image_version: 0.26.1-2-20250907
    # common sidecar images versions
    # TODO: autogenerate this from the registry.
    common_images:
      httpd:
        exporter: prometheus-apache-exporter:0.0.4-20240623
      mcrouter:
        mcrouter: mcrouter:2023.07.17.00-1-20240714
        exporter: prometheus-mcrouter-exporter:0.0.1-3-20240714
      nutcracker:
        nutcracker: nutcracker:0.0.5-20240825
        exporter: prometheus-nutcracker-exporter:0.0.2-20211010
      rsyslogd: rsyslog:8.2102.0-3
      statsd:
        exporter: prometheus-statsd-exporter:0.26.1-2-20250907
      postgresql:
        "15": 2025-08-06-145838-e8a1b6ac1f969432aa274426a7db1baf8b02cb21@sha256:29254e052217729cbd96562e8d35827739ca6375a047653e1e206539d570fd87
      postgresql-documentdb:
        "17": 2025-10-24-084441-747c01dd2c614a9534f5dec8ba19309468a443f4@sha256:86ea4cdc7c894b0c581b95264d3d4b587b15eec455792da215c0e3d060267ddd
  eqiad:
    mesh:
      certmanager: &certmanager_eqiad
        enabled: true
        domains:
          - discovery.wmnet
          - svc.eqiad.wmnet
  codfw:
    mesh:
      certmanager: &certmanager_codfw
        enabled: true
        domains:
          - discovery.wmnet
          - svc.codfw.wmnet
  staging-eqiad:
    mesh:
      certmanager: &wikikube_staging_certmanager
        enabled: true
        # We have a wildcard DNS entry for *.k8s-staging.discovery.wmnet
        # used for ingress, but we can reuse that for NodePort services as well.
        domains:
          - k8s-staging.discovery.wmnet
        # Staging used a generic cergen cert with the following SAN
        # so we keep them for backwards compatibility.
        extraFQDNs:
          - staging.svc.eqiad.wmnet
  staging-codfw:
    mesh:
      certmanager:
        <<: *wikikube_staging_certmanager
        extraFQDNs:
          - staging.svc.codfw.wmnet
  ml-serve-eqiad:
    mesh:
      certmanager:
        <<: *certmanager_eqiad
  ml-serve-codfw:
    mesh:
      certmanager:
        <<: *certmanager_codfw
  ml-staging-codfw:
    mesh:
      certmanager:
        enabled: true
        # We have a wildcard DNS entry for *.k8s-ml-staging.discovery.wmnet
        # used for ingress, but we can reuse that for NodePort services as well.
        domains:
          - k8s-ml-staging.discovery.wmnet
        # Staging used a generic cergen cert with the following SAN
        # so we keep them for backwards compatibility.
        extraFQDNs:
          - ml-staging.svc.eqiad.wmnet
          - ml-staging.svc.eqiad.wmnet
  dse-k8s-eqiad:
    mesh:
      certmanager:
        <<: *certmanager_eqiad
  aux-k8s-eqiad:
    mesh:
      certmanager:
        <<: *certmanager_eqiad
  aux-k8s-codfw:
    mesh:
      certmanager:
        <<: *certmanager_codfw

profile::mediawiki::deployment::server::rsync_host: "deployment.%{::site}.wmnet"
profile::mediawiki::apc_shm_size: 128M

profile::contacts::role_contacts: ["ServiceOps"]
profile::base::production::role_description: 'Deployment server for all Wikimedia services'

# enable docker with the desired settings
profile::docker::engine::declare_service: false
profile::docker::engine::settings:
  data-root: "/srv/docker"
  live-restore: true
  iptables: false
  storage-driver: "overlay2"
profile::ci::pipeline::publisher::docker_registry_user: "ci-restricted"
# Prune at 3 days old instead of 14
docker::prune_old_images::older_than: 3

profile::monitoring::nrpe_check_disk_options: -w 10% -c 5% -W 6% -K 3% -l -e -A -i '/(var/lib|run)/(docker|kubelet)/*' --exclude-type=tracefs --exclude-type=overlay

# The deployment group for mediawiki is called "deployment".
# See T303857
deployment_group: "deployment"

# T310395
profile::mediawiki::deployment::server::enable_auto_deploy: true
# See https://releases-jenkins.wikimedia.org/job/Automatic%20branch%20cut/configure for the schedule
# of the branch cut job that auto deployment relies on.
profile::mediawiki::deployment::server::auto_deploy_interval: Mon *-*-* 20:00:00 America/Los_Angeles
profile::mediawiki::deployment::server::auto_clean_interval: Mon *-*-* 21:00:00 America/Los_Angeles
profile::mediawiki::deployment::server::scap_clean_images_interval: Mon *-*-* 21:30:00 America/Los_Angeles
profile::mediawiki::deployment::server::pretrain_interval: "*-*-* 02:00:00 UTC"
# T367118 Add conftool-data for consumption by helmfile for maintenance jobs control
profile::confd::instances:
  main:
    interval: 30

# T383945
profile::mediawiki::deployment::server::ensure_spiderpig: present
profile::envoy::ensure: present
profile::tlsproxy::envoy::ssl_provider: cfssl
profile::tlsproxy::envoy::sni_support: "yes"
profile::tlsproxy::envoy::services:
   - server_names: ["spiderpig.wikimedia.org"]
     port: 9000
     cert_name: "spiderpig.wikimedia.org"
profile::tlsproxy::envoy::global_cert_name: "deployment.eqiad.wmnet"
# Unsetting the kubernetes client version will make it install kubectl versions matching all k8s versions
# we are currently using.
profile::kubernetes::client::version: ~
