druid::broker::env: {DRUID_EXTRA_JVM_OPTS: '-XX:NewSize=6g -XX:MaxNewSize=6g -XX:MaxDirectMemorySize=64g
    -XX:+UseConcMarkSweepGC -XX:+PrintGCDetails -XX:+PrintGCTimeStamps', DRUID_HEAP_OPTS: -Xmx25g
    -Xms25g}
druid::broker::properties: {druid.broker.http.numConnections: 20, druid.broker.http.readTimeout: PT5M,
  druid.processing.buffer.sizeBytes: 2147483647, druid.processing.numThreads: 10,
  druid.server.http.numThreads: 20}
