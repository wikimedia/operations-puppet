druid::properties: {druid.metadata.storage.connector.host: analytics1003.eqiad.wmnet,
  druid.metadata.storage.type: mysql, druid.request.logging.dir: /var/log/druid, druid.request.logging.type: file,
  druid.storage.type: hdfs}
druid::use_cdh: true
