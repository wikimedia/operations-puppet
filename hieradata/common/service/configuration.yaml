service::configuration::http_proxy: "http://urldownloader.svc.%{::site}.wmnet:8080"
service::configuration::statsd_host: statsd.eqiad.wmnet # statsd is (still) eqiad-only
service::configuration::logstash_host: logstash.svc.eqiad.wmnet # logstash is (still) eqiad-only
