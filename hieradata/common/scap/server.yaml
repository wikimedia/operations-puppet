scap::server::keyholder_agents:
  analytics_deploy:
    trusted_groups: [analytics-admins]
  deploy-service:
    trusted_groups: [deploy-service, aqs-admins, deploy-aqs]
  dumpsdeploy:
    trusted_groups: [ops]
  eventlogging:
    trusted_groups: [eventlogging-admins]
  phabricator:
    trusted_groups: [deploy-phabricator]
scap::server::sources:
  analytics/refinery: {repository: analytics/refinery, scap_repository: analytics/refinery/scap}
  cxserver/deploy: {repository: cxserver/deploy}
  dumps/dumps: {repository: operations/dumps, scap_repository: operations/dumps/scap}
  eventlogging/analytics: {repository: eventlogging, scap_repository: eventlogging/scap/analytics}
  eventlogging/eventbus: {repository: eventlogging, scap_repository: eventlogging/scap/eventbus}
  graphoid/deploy: {}
  kartotherian/deploy: {}
  mobileapps/deploy: {}
  ores/deploy: {repository: mediawiki/services/ores/deploy}
  parsoid/deploy: {}
  phabricator/deployment: {repository: phabricator/deployment}
  striker/deploy: {repository: labs/striker/deploy}
  tilerator/deploy: {}
  wdqs/wdqs: {repository: wikidata/query/deploy}
