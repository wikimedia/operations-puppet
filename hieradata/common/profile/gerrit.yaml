profile::gerrit::ipv6: ~
profile::gerrit::enable_monitoring: true

profile::gerrit::mask_service: false

profile::gerrit::host: 'gerrit.wikimedia.org'
profile::gerrit::config: 'gerrit.config.erb'

profile::gerrit::active_host: 'gerrit1003.wikimedia.org'
profile::gerrit::replica_hosts:
    - 'gerrit-replica.wikimedia.org'

profile::gerrit::migration::data_dir: '/srv/gerrit'

profile::gerrit::lfs_replica_sync: true
profile::gerrit::lfs_sync_dest:
    # - 'gerrit1003.wikimedia.org'
    - 'gerrit2002.wikimedia.org'
    - 'gerrit2003.wikimedia.org'

# External backups
profile::gerrit::backups_enabled: true
profile::gerrit::backup_set: 'gerrit-repo-data'

# SSL/TLS certs
profile::gerrit::use_acmechief: true

## Deployment
profile::gerrit::daemon_user: 'gerrit2'
profile::gerrit::gerrit_site: "/var/lib/gerrit2/review_site"
profile::gerrit::scap_user: 'gerrit-deploy'
profile::gerrit::manage_scap_user: true
profile::gerrit::scap_key_name: 'gerrit'

# List of gerrit servers used in firewall rules.
profile::gerrit::ssh_allowed_hosts:
 - gerrit1003.wikimedia.org

# Assume all production nodes are replicas
profile::gerrit::git_dir: '/srv/gerrit/git'

profile::gerrit::ssh_host_key: 'ssh_host_key'
profile::gerrit::replication:
    github:
        url: 'git@github.com:wikimedia/${name}'
        authGroup: 'mediawiki-replication'
        remoteNameStyle: 'dash'
        mirror: false
        push:
            - '+refs/heads/*:refs/heads/*'
            - '+refs/tags/*:refs/tags/*'
        createMissingRepositories: false
        threads: 2
        maxRetries: 50
        rescheduleDelay: 15
        replicatePermissions: false
        # We drop the whole apps/ hierarchy that has been entirely archived.
        # Notes:
        # - double escape backslashes: once for regex, once for gitconfig
        #   formatting
        # - there can be ONLY one inverted match, else the second will match
        #   the one excluded by the first - T392198
        # You are better of reparenting the project to All-Archived-Projects
        # which BLOCK Read for the group mediawiki-replication. This is only
        # kept to ensure we do not replicate the GitHub apps.
        projects:
            - '^(?:(?!apps\\/).)*$'
    replica_codfw:
        url: "%{lookup('profile::gerrit::daemon_user')}@gerrit2002.wikimedia.org:/srv/gerrit/git/${name}.git"
        mirror: true
        replicateProjectDeletions: true
        replicateHiddenProjects: true
        defaultForceUpdate: true
        threads: 4
        replicationDelay: 5
        rescheduleDelay: 5

profile::gerrit::gerrit_abusers:
  - '167.99.209.130/32'
  - '178.62.222.136/32'
  - '47.74.0.0/15'  # Alibaba
  - '47.76.0.0/14'  # Alibaba
  - '47.80.0.0/13'  # Alibaba
  - '51.68.11.191/32'
  - '101.47.0.0/18'  # ByteDance crawling network - T375996
  - '43.133.0.0/16' # Tencent
  - '43.134.0.0/16' # Tencent
  - '43.159.0.0/18' # Tencent
  - '20.125.66.80/28' # openai-gptbot
  - '20.171.206.0/23' # openai-gptbot
  - '4.227.36.0/25' # openai-gptbot
  - '52.230.152.0/24' # openai-gptbot
  - '45.197.1.0/24' # Zillion
  - '2a0a:4cc0::/40' # Netcup
  - '2a03:4000::/32' # Netcup
  - '85.208.98.18/32' 
  - '89.58.0.0/18' # Netcup
  - '152.53.0.0/16' # ANX Holding GmbH
  - '37.120.184.0/24' # Netcup
  - '37.120.185.0/24' # Netcup
  - '37.120.187.0/24' # Netcup
  - '209.209.9.152/32'
  - '1.24.0.0/13' # starting here: lots of hits in  nftables tracking table, mostly CN
  - '1.188.0.0/14'
  - '27.152.0.0/13'
  - '27.224.0.0/14'
  - '27.224.0.0/14'
  - '36.40.0.0/13'
  - '36.128.0.0/10'
  - '42.63.0.0/16'
  - '42.88.0.0/13'
  - '42.225.164.107'
  - '42.248.0.0/13'
  - '49.64.0.0/11'
  - '60.8.0.0/15'
  - '60.14.0.0/15'
  - '101.16.0.0/12'
  - '111.0.0.0/10'
  - '111.119.208.0/20'
  - '120.192.0.0/11'
  - '170.244.72.0/22'
  - '183.192.0.0/11'
  - '223.64.0.0/11'
  - '223.144.0.0/12'