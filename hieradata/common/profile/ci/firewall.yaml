# Hosts running zuul-merger process
#
# Use ip addresses since the values are injected in iptables rules.
#
profile::ci::firewall::zuul_merger_hosts:
    - 208.80.154.132 # contint1002.wikimedia.org
    - 208.80.153.39  # contint2002.wikimedia.org

profile::ci::firewall::jenkins_host: "%{alias('profile::ci::jenkins::jenkins_host')}"
profile::ci::firewall::zuul_scheduler_host: "%{alias('profile::ci::jenkins::zuul_scheduler_host')}"
