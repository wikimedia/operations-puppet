profile::openstack::base::trove::db_user: 'trove'
profile::openstack::base::trove::api_bind_port: 18779
profile::openstack::base::trove::rabbit_user: 'trove'

# A different rabbit user exclusively for communication between
# the trove guest agent and the trove service
profile::openstack::base::trove::trove_guest_rabbit_user: 'troveguest'
