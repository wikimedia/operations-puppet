<%#- SPDX-License-Identifier: Apache-2.0 -%>
# This file is managed by puppet
<%-
@known_hosts.each do |title, parameters|
  aliases = parameters.fetch('host_aliases', []).insert(0, title)
-%>
<%= aliases.join(',') %> <%= parameters['type'] %> <%= parameters['key'] %>
<%- end -%>
