CONFIG = {
    'mode': 'wsgi',
    'working_dir': '<%= @directory %>',
    'args': (
        '--bind=0.0.0.0:<%= @port %>',
        '--workers=4',
        '--timeout=10',
        'servermon.wsgi:application',
    ),
}
