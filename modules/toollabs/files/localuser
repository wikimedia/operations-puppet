#!/bin/bash
#
# THIS FILE IS MAINTAINED BY PUPPET
# source: modules/toollabs/files/localuser
# from:   toollabs::mailrelay
#

/usr/bin/ldapsearch -b "uid=$1,ou=people,dc=wikimedia,dc=org" -LLL -o ldif-wrap=no -x mail | /bin/sed -ne 's/^mail: //p;'
