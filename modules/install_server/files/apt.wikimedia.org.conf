server {
   listen 80;
   listen [::]:80 ipv6only=on;
   server_name apt.wikimedia.org;

   root /srv;

   location / {
      index index.html index.htm;
      autoindex on;
      charset utf-8;
   }
}
