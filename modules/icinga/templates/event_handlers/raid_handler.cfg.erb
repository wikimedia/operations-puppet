define command{
    command_name    raid_handler
    command_line    $USER1$/eventhandlers/raid_handler -s $SERVICESTATE$ -t $SERVICESTATETYPE$ -a $SERVICEATTEMPT$ -H $HOSTADDRESS$ -r $ARG1$ -D "$ARG2$" -c $ARG3$
    }
