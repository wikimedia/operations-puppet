define command{
    command_name    raid_handler
    command_line    $USER1$/eventhandlers/raid_handler -d -s $SERVICESTATE$ -t $SERVICESTATETYPE$ -a $SERVICEATTEMPT$ -H $HOSTNAME$ -m "$SERVICEOUTPUT$" -r $ARG1$ -D "$ARG2$" -c $ARG3$
    }
