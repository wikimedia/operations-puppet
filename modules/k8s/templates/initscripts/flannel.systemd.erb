[Unit]
Description=flannel overlay network

[Service]
ExecStart=/usr/bin/flanneld \
    --etcd-endpoints=<%= @etcd_endpoints %> \
    --etcd-cafile=/etc/ssl/certs/puppet.pem

[Install]
WantedBy=multi-user.target
