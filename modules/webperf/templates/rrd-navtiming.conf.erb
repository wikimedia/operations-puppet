# rrd-navtiming

description "NavigationTiming RRD logger"

start on (local-filesystems and net-device-up IFACE!=lo)

setuid webperf
setgid webperf

respawn
respawn limit unlimited

exec /srv/webperf/rrd-navtiming         \
    --endpoint "<%= @endpoint %>"       \
    --log-path "/var/log/rrd-navtiming" \
    "/var/lib/rrd-navtiming/navtiming.rrd"
