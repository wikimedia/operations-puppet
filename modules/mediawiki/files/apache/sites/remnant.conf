# Meta
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/meta"
    ServerName meta.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteRule . - [E=RW_PROTO:%{HTTP:X-Forwarded-Proto}]
    RewriteCond %{ENV:RW_PROTO} !=https
    RewriteRule . - [E=RW_PROTO:http]

    # Make robots.txt editable via Mediawiki:robots.txt
    RewriteRule ^/robots.txt$ /w/robots.php [L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/meta/w/index.php
    RewriteRule ^/$ /w/index.php

    # UseMod compatibility URLs
    RewriteCond %{QUERY_STRING} ([^&;]+)
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php?title=%1 [R=301,L]

    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php [R=301,L]

    # Early phase 2 compatibility URLs
    RewriteRule ^/wiki\.phtml$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php [R=301,L]

    RewriteRule ^/math/(.*) %{ENV:RW_PROTO}://upload.wikimedia.org/math/$1 [R=301]

    # Uploads are offsite
    RewriteRule ^/upload/(.*)$ %{ENV:RW_PROTO}://upload.wikimedia.org/wikipedia/meta/$1 [R=302]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/meta/w">
        <IfModule mod_php5.c>
          php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/meta/w/extensions">
        <IfModule mod_php5.c>
          php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Wikisource
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/sources"
    ServerName wikisource.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteRule . - [E=RW_PROTO:%{HTTP:X-Forwarded-Proto}]
    RewriteCond %{ENV:RW_PROTO} !=https
    RewriteRule . - [E=RW_PROTO:http]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/sources/w/index.php
    RewriteRule ^/$ /w/index.php

    # UseMod compatibility URLs
    RewriteCond %{QUERY_STRING} ([^&;]+)
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php?title=%1 [R=301,L]
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php [R=301,L]

    RewriteRule ^/math/(.*) %{ENV:RW_PROTO}://upload.wikimedia.org/math/$1 [R=301]

    # Uploads are offsite
    RewriteRule ^/upload/(.*)$ %{ENV:RW_PROTO}://upload.wikimedia.org/wikipedia/sources/$1 [R=302]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    # Load robots.txt from the wiki
    RewriteRule ^/robots.txt$ /w/robots.php [L]

    <Directory "/srv/mediawiki/docroot/sources/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/sources/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Wikimedia Commons
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/commons"
    ServerName commons.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteRule . - [E=RW_PROTO:%{HTTP:X-Forwarded-Proto}]
    RewriteCond %{ENV:RW_PROTO} !=https
    RewriteRule . - [E=RW_PROTO:http]

    # Make robots.txt editable via Mediawiki:robots.txt
    RewriteRule ^/robots.txt$ /w/robots.php [L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/commons/w/index.php
    RewriteRule ^/$ /w/index.php

    # UseMod compatibility URLs
    RewriteCond %{QUERY_STRING} ([^&;]+)
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php?title=%1 [R=301,L]
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php [R=301,L]

    RewriteRule ^/math/(.*) %{ENV:RW_PROTO}://upload.wikimedia.org/math/$1 [R=301]

    # Uploads are offsite
    RewriteRule ^/upload/(.*)$ %{ENV:RW_PROTO}://upload.wikimedia.org/wikipedia/commons/$1 [R=302]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/commons/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/commons/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Internal grants wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/grants"
    ServerName grants.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://grants.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/grants/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/grants/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/grants/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# fdc.wikimedia.org - Funds Dissemination Committee Wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/fdc"
    ServerName fdc.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://fdc.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/fdc/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/fdc/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/fdc/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Internal wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/internal"
    ServerName internal.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://internal.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/internal/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/internal/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/internal/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Board wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/board"
    ServerName board.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://board.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/board/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/board/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/board/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Board Governance Committee
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/boardgovcom"
    ServerName boardgovcom.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://boardgovcom.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/boardgovcom/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/boardgovcom/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/boardgovcom/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Spcom wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/spcom"
    ServerName spcom.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://spcom.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/spcom/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/spcom/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/spcom/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Chapcom wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/chapcom"
    ServerName chapcom.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://chapcom.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/chapcom/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/chapcom/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/chapcom/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Incubator wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/incubator"
    ServerName incubator.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteRule . - [E=RW_PROTO:%{HTTP:X-Forwarded-Proto}]
    RewriteCond %{ENV:RW_PROTO} !=https
    RewriteRule . - [E=RW_PROTO:http]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/incubator/w/index.php
    RewriteRule ^/$ /w/index.php

    # UseMod compatibility URLs
    RewriteCond %{QUERY_STRING} ([^&;]+)
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php?title=%1 [R=301,L]
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php [R=301,L]

    RewriteRule ^/math/(.*) %{ENV:RW_PROTO}://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/incubator/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/incubator/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# WikiSpecies wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/species"
    ServerName species.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteRule . - [E=RW_PROTO:%{HTTP:X-Forwarded-Proto}]
    RewriteCond %{ENV:RW_PROTO} !=https
    RewriteRule . - [E=RW_PROTO:http]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/species/w/index.php
    RewriteRule ^/$ /w/index.php

    # UseMod compatibility URLs
    RewriteCond %{QUERY_STRING} ([^&;]+)
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php?title=%1 [R=301,L]
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php [R=301,L]

    RewriteRule ^/math/(.*) %{ENV:RW_PROTO}://upload.wikimedia.org/math/$1 [R=301]

    # Uploads are offsite
    RewriteRule ^/upload/(.*)$ %{ENV:RW_PROTO}://upload.wikimedia.org/wikipedia/species/$1 [R=302]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    # Load robots.txt from the wiki
    RewriteRule ^/robots.txt$ /w/robots.php [L]

    <Directory "/srv/mediawiki/docroot/species/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/species/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# search committee private wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/searchcom"
    ServerName searchcom.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://searchcom.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/searchcom/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/searchcom/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/searchcom/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Usability Wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/usability"
    ServerName usability.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteRule . - [E=RW_PROTO:%{HTTP:X-Forwarded-Proto}]
    RewriteCond %{ENV:RW_PROTO} !=https
    RewriteRule . - [E=RW_PROTO:http]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/usability/w/index.php
    RewriteRule ^/$ /w/index.php

    # UseMod compatibility URLs
    RewriteCond %{QUERY_STRING} ([^&;]+)
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php?title=%1 [R=301,L]
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php [R=301,L]

    RewriteRule ^/math/(.*) %{ENV:RW_PROTO}://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    # Load robots.txt from the wiki
    RewriteRule ^/robots.txt$ /w/robots.php [L]

    <Directory "/srv/mediawiki/docroot/usability/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/usability/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# strategy  wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/strategy"
    ServerName strategy.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteRule . - [E=RW_PROTO:%{HTTP:X-Forwarded-Proto}]
    RewriteCond %{ENV:RW_PROTO} !=https
    RewriteRule . - [E=RW_PROTO:http]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/strategy/w/index.php
    RewriteRule ^/$ /w/index.php

    # UseMod compatibility URLs
    RewriteCond %{QUERY_STRING} ([^&;]+)
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php?title=%1 [R=301,L]
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php [R=301,L]

    RewriteRule ^/math/(.*) %{ENV:RW_PROTO}://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/strategy/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/strategy/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# office private wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/office"
    ServerName office.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://office.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/office/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/office/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/office/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>

</VirtualHost>

# chair private wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/chair"
    ServerName chair.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://chair.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/chair/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/chair/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/chair/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# advisory board PUBLIC wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/advisory"
    ServerName advisory.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/advisory/w/index.php
    RewriteRule ^/$ /w/index.php

    # UseMod compatibility URLs
    RewriteCond %{QUERY_STRING} ([^&;]+)
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php?title=%1 [R=301,L]
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php [R=301,L]

    RewriteRule ^/math/(.*) %{ENV:RW_PROTO}://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    # Load robots.txt from the wiki
    RewriteRule ^/robots.txt$ /w/robots.php [L]

    <Directory "/srv/mediawiki/docroot/advisory/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/advisory/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# auditcom private wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/auditcom"
    ServerName auditcom.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://auditcom.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/auditcom/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/auditcom/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/auditcom/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>


# quality private wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/quality"
    ServerName quality.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/quality/w/index.php
    RewriteRule ^/$ /w/index.php

    # UseMod compatibility URLs
    RewriteCond %{QUERY_STRING} ([^&;]+)
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php?title=%1 [R=301,L]
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php [R=301,L]

    RewriteRule ^/math/(.*) %{ENV:RW_PROTO}://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/quality/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/quality/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>


# OTRS private wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/otrs-wiki"
    ServerName otrs-wiki.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://otrs-wiki.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/otrs-wiki/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/otrs-wiki/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/otrs-wiki/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Executive director's private wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/exec"
    ServerName exec.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://exec.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/exec/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/exec/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/exec/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Private wiki for Jay and his minions
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/collab"
    ServerName collab.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://collab.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/collab/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/collab/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/collab/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# Outreach Wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/outreach"
    ServerName outreach.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/outreach/w/index.php
    RewriteRule ^/$ /w/index.php

    # UseMod compatibility URLs
    RewriteCond %{QUERY_STRING} ([^&;]+)
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php?title=%1 [R=301,L]
    RewriteRule ^/wiki\.cgi$ %{ENV:RW_PROTO}://%{SERVER_NAME}/w/index.php [R=301,L]

    RewriteRule ^/math/(.*) %{ENV:RW_PROTO}://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/outreach/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/outreach/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

#movementroles wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/movementroles"
    ServerName movementroles.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://movementroles.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/movementroles/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/movementroles/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/movementroles/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# checkuser private wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/checkuser"
    ServerName checkuser.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://checkuser.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/checkuser/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/checkuser/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/checkuser/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# steward private wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/steward"
    ServerName steward.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://steward.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/steward/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/steward/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/steward/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

# ombudsmen private wiki
<VirtualHost *:80>
    DocumentRoot "/srv/mediawiki/docroot/ombudsmen"
    ServerName ombudsmen.wikimedia.org

    AllowEncodedSlashes On

    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteRule ^/(.*)$ https://ombudsmen.wikimedia.org/$1 [R=301,L]

    # Primary wiki redirector:
    Alias /wiki /srv/mediawiki/docroot/ombudsmen/w/index.php
    RewriteRule ^/$ /w/index.php

    RewriteRule ^/math/(.*) https://upload.wikimedia.org/math/$1 [R=301]

    # Configurable favicon
    RewriteRule ^/favicon\.ico$ /w/favicon.php [L]

    <Directory "/srv/mediawiki/docroot/ombudsmen/w">
        <IfModule mod_php5.c>
            php_admin_flag engine on
        </IfModule>
    </Directory>
    <Directory "/srv/mediawiki/docroot/ombudsmen/w/extensions">
        <IfModule mod_php5.c>
            php_admin_flag engine off
        </IfModule>
    </Directory>
</VirtualHost>

## donatewiki has been moved to main.conf so it can catch donate.wikipedia.org
