# changes/server
#
# This is an Upstart job configuration file for an Changes server.
# Instances of this job are started by the changes/init task.
#
description "Changes server instance"

instance $server

stop on changes.stop

setuid changes
setgid changes

exec /usr/local/bin/changes $server $redis

respawn
