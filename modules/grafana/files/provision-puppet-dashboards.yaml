# Load the Puppet-provisioned dashboard JSON files.
# See https://docs.grafana.org/administration/provisioning/#dashboards

apiVersion: 1

providers:
  - name: 'puppet'
    orgId: 1
    folder: ''
    type: file
    options:
      path: /var/lib/grafana/dashboards