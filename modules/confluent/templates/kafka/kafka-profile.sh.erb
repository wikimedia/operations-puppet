# NOTE:  This file is managed by Puppet

export ZOOKEEPER_URL=<%= @zookeeper_connect %>
export BROKER_LIST=<%= @brokers.map { |host, conf| "#{host}:#{conf['port'] || @default_port}" }.sort.join(',') %>
