# THIS FILE IS MANAGED BY UPPET

check host etherpad with address 127.0.0.1
    start "/usr/sbin/service apache2 start"
    stop "/usr/sbin/service apache2 stop"
    if failed
        port 80
        protocol HTTP
        request /
        with timeout 10 seconds
        with http headers [Host: etherpad.wikimedia.org, X-Forwarded-Proto: https]
        then restart
