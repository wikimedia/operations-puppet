# Discovery objects. they will represent a sequence of cluster/service/dc combinations
discovery:
  path: "discovery"
  depends:
    - service
  tags:
    - dnsdisc
  schema:
    pooled:
      type: "bool"
      default: false
    references:
      type: "list"
      default: []
