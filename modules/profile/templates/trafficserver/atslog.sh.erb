#!/bin/sh
<%#- SPDX-License-Identifier: Apache-2.0 -%>
# atslog -- print ATS <%= @atslog_filename %> logs to standard output

socket="<%= @paths['runtimedir'] %>/<%= @atslog_filename %>.sock"
regexp="${1:- }"

fifo-log-tailer -socket "$socket" -regexp "$regexp"
