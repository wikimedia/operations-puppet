<%#- SPDX-License-Identifier: Apache-2.0 -%>
<%- nnodes = @facts['numa']['device_to_node'][@numa_iface].join(',') -%>
[Service]
ExecStart=
ExecStart=/usr/bin/numactl -a -N <%= nnodes %> /usr/sbin/nginx -g 'daemon on; master_process on;'
