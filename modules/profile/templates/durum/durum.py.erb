# This file is managed by Puppet [see profile/durum/durum.py.erb].

import uuid

from flask import Flask, render_template


def create_app():
    app = Flask(__name__)

    @app.route("/")
    def landing():
        return render_template("index.html", request={"uuid": uuid.uuid4()})

    return app


app = create_app()
