# Cumin Masters. TODO: use 'restrict' once available across the fleet (> jessie)
from="<%= @ssh_authorized_sources %>",no-agent-forwarding,<% if @cumin_auth_group == 'cumin_masters' %>no-port-forwarding,<% end %>no-x11-forwarding,no-user-rc <%= @cumin_master_pub_key %>
