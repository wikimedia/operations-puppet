<%
require 'digest/sha1'
require 'base64'
require 'securerandom'
salt = SecureRandom.base64 5

@acls[@title].each do |user| %>
<%= user %>:<%= function_htpasswd([ @userss[user], @salt]) %>
<%- end -%>
