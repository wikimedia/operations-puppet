<?php
/**
 * Configuration for XHGui.  Values from config.php.default are used
 * unless overridden here.
 */

return array(
    'save.handler' => 'pdo',

    'pdo' => array(
        'dsn' => 'mysql:host=<%= @mysql_host %>;dbname=<%= @mysql_db %>;charset=utf8',
        'user' => '<%= @mysql_user %>',
        'pass' => '<%= @mysql_password %>',
        'table' => 'xhgui'
    ),

    // These are ignored if save.handler != mongodb, but just in case:
    'db.host' => '',
    'db.db' => ''
);
