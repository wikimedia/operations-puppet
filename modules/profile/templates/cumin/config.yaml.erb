backend: puppetdb
transport: clustershell
log_file: <%= @cumin_log_path %>/cumin.log

puppetdb:
    host: <%= @puppetdb_host %>
    port: 443

clustershell:
    environment:
        SSH_AUTH_SOCK: /run/keyholder/proxy.sock
