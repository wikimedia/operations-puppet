# Managed by puppet
[Unit]
Description=toolforge bigbrother service, watches jobs and restarts them

[Service]
Type=simple
Restart=always
ExecStart=/usr/local/sbin/bigbrother
ProtectSystem=full
ProtectHome=read-only
PrivateTmp=true
StandardOutput=null

[Install]
WantedBy=multi-user.target
