[Unit]
Description="HAProxy mtail %i instance"
After=haproxy.service
Requires=haproxy.service

[Service]
SyslogIdentifier=haproxymtail-%i
Restart=always
ExecStart=/usr/local/bin/haproxymtail <%= @mtail_dir %> <%= @mtail_port %>

[Install]
WantedBy=multi-user.target
