#!/bin/sh

set -x
set -e

SERVICE=$1
NAMESPACE=$SERVICE

for i in ${SERVICE}-eqiad ${SERVICE}-codfw ${SERVICE}-staging ; do
	KUBECONFIG="/etc/kubernetes/${i}.config" helm install --tiller-namespace=${NAMESPACE} ${SERVICE}
done
