{{ range gets "/*/*" }}{{ $dnsdisc := dir .Key  }}{{ $dc := base .Key }}{{ $vals := json .Value }}
- dnsdisc: "{{ $dnsdisc }}"
  datacenter: "{{ $dc }}"
  pooled: {{ index $vals "pooled" }}
  services: "{{ index $vals "references" }}"{{ end }}
