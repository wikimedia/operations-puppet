<%#- SPDX-License-Identifier: Apache-2.0 -%>
NAMESERVERS="{{ range $index, $element := gets "/*/dnsbox/authdns-update/*" }}{{ $host := json .Value }}{{ if eq $host.pooled "yes" }}{{ if $index }} {{ end }}{{ base .Key }}{{ end }}{{ end }}"
FQDN="<%= @fqdn %>"
WORKINGDIR=<%= @workingdir %>
ORIGIN="<%= @gitrepo %>"
NETBOXDNS="<%= @netbox_dns_snippets_dir %>"
