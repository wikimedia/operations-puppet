<%#- SPDX-License-Identifier: Apache-2.0 -%>
NAMESERVERS="{{ range $index, $element := gets "/authdns/*/*" }}{{ $host := json .Value }}{{ if ne $host.pooled "inactive" }}{{ if $index }} {{ end }}{{ base .Key }}{{ end }}{{ end }}"
FQDN="<%= @fqdn %>"
WORKINGDIR=<%= @workingdir %>
ORIGIN="<%= @gitrepo %>"
NETBOXDNS="<%= @netbox_dns_snippets_dir %>"
