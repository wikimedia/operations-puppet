<%#- SPDX-License-Identifier: Apache-2.0 -%>
<% @discovery_services.map{ |data| data['dnsdisc']}.sort.each do |svc| -%>
discovery-state-<%= svc %> => {
    plugin => extfile,
    file   => /var/lib/gdnsd/discovery-<%= svc %>.state,
    direct => true,
}

<% end -%>

discovery-state-failoid => {
    plugin => static,
    state => UP,
    ttl => 10,
}
