ceph_pool: <%= @ceph_vm_pool %>
exclude_servers:
 admin:
   - ".*"
 admin-monitoring:
   - ".*"
 cloudvirt-canary:
   - ".*"
 integration:
   - "^integration-agent.*"
 mwoffliner:
   - ".*"
 tools:
   - "^.*worker.*$"
   - "tools-sgeexec-.*$"
   - "^.*sgewebgrid-lighttpd.*$"
 video:
   - "encoding.*$"

live_for_days: 7
