ceph_pool: <%= @ceph_vm_pool %>
exclude_servers:
 admin:
   - ".*"
 admin-monitoring:
   - ".*"
 cloudvirt-canary:
   - ".*"
 tools:
   - "^.*worker.*$"
   - "tools-sgeexec-.*$"
   - "^.*sgewebgrid-lighttpd.*$"
live_for_days: 3
