ceph_pool: <%= @ceph_vm_pool %>
exclude_servers:
 admin:
   - ".*"
 admin-monitoring:
   - ".*"
 cloudvirt-canary:
   - ".*"
 integration:
   - "^integration-agent.*$"
 mwoffliner:
   - ".*"
 paws:
   - "^paws-k8s-worker.*$"
 sre-sandbox:
   - ".*"
 tools:
   - "^.*worker.*$"
   - "^tools-sgeexec-.*$"
   - "^.*sgewebgrid-lighttpd.*$"
 toolsbeta:
   - "^.*worker.*$"
   - "^tools-sgeexec-.*$"
   - "^.*sgewebgrid-lighttpd.*$"
 transferpy-test:
   - ".*"
 video:
   - "encoding.*$"

live_for_days: 7
