ceph_pool: <%= @ceph_vm_pool %>
exclude_servers:
 admin:
   - ".*"
 admin-monitoring:
   - ".*"
 cloudvirt-canary:
   - ".*"
 codesearch:
   - ".*"
 deployment-prep:
   - "^.*snapshot02.*$"
   - '^deployment-docker.*$
   - '^deployment-elastic.*$
   - '^deployment-mediawiki-.*'
   - '^deployment-mwmaint.*'
 extdist:
   - ".*"
 integration:
   - "^integration-agent.*$"
 mwoffliner:
   - ".*"
 paws:
   - "^paws-k8s-worker.*$"
 sre-sandbox:
   - ".*"
 testlabs:
   - ".*"
 tools:
   - "^.*worker.*$"
   - "^tools-sgeexec-.*$"
   - "^.*sgewebgrid-lighttpd.*$"
 toolsbeta:
   - ".*"
 transferpy-test:
   - ".*"
 video:
   - "encoding.*$"

live_for_days: 4
