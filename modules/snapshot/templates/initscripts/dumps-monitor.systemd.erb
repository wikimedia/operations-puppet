#####################################################################
### THIS FILE IS MANAGED BY PUPPET
#####################################################################

[Unit]
Description=Dumps monitoring service
After=network.target

[Service]
User=datasets
WorkingDirectory=<%= scope.lookupvar('snapshot::deployment::dirs::repodir') -%>
ExecStart=/bin/bash <%= scope.lookupvar('snapshot::deployment::dirs::repodir') -%>/monitor --configfile confs/wikidump.conf.monitor

[Install]
WantedBy=multi-user.target
