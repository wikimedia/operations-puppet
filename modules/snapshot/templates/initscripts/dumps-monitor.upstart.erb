#####################################################################
### THIS FILE IS MANAGED BY PUPPET
#####################################################################

description "Dumps monitoring service"

start on (local-filesystems and net-device-up IFACE!=lo)
stop on runlevel [!2345]

setuid "datasets"
setgid "datasets"

respawn

chdir <%= scope.lookupvar('snapshot::deployment::dirs::repodir') -%>

exec /bin/bash <%= scope.lookupvar('snapshot::deployment::dirs::repodir') -%>/monitor --configfile confs/wikidump.conf.monitor
