<%
  targets_config = []
  targets_config.push({
    labels  => @labels,
    targets => @hosts[@site][@cluster].map{ |x| "#{x}:#{@port}" }
  })
-%>

<%= scope.function_ordered_yaml(targets_config) -%>
