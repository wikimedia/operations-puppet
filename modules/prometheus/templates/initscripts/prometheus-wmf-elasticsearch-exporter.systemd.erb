[Unit]
Description=Prometheus WMF Elasticsearch Exporter

[Service]
Restart=always
User=prometheus
ExecStart=/usr/local/bin/prometheus-wmf-elasticsearch-exporter --listen :<%= @prometheus_port %> --port <%= @elasticsearch_port %>

[Install]
WantedBy=multi-user.target
