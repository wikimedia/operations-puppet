[Unit]
Description=Prometheus exporter for Haproxy

[Service]
Restart=always
User=prometheus
Group=prometheus
ExecStart=/usr/bin/haproxy_exporter --haproxy.scrape-uri=<%= @endpoint %> --web.listen-address :<%= @listen_port %>

[Install]
WantedBy=multi-user.target
