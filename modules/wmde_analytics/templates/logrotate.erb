# This file is managed by Puppet.
# puppet:///modules/wmde_analytics/logrotate
# Rotate wmde_analytics cron output logs
#
<%= @log_dir %>/*.log {
       notifempty
       maxage 10
       rotate 2
       dateext
       compress
       missingok
}
