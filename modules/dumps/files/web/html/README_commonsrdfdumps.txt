These files contain structured data from Commons in rdf format, as
ttl and nt files.

Prefixes defined in dumps are valid only in a local context of the
specific dump and may change in the future. Any scripts you write
to process these dumps should take that into account.
