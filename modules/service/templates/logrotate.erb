# logrotate(8) config for <%= @title %>

<%= @local_logdir %>/* {
    daily
    copytruncate
    missingok
    compress
    delaycompress
    notifempty
    rotate 15
    postrotate
	    invoke-rc.d <%= @title %> reload
    endscript
}
