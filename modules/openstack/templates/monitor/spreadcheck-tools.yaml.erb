project: tools
classifier:
  webproxy-: webproxy
  checker-: checker
  redis-: redis
  services-: services
  bastion-: bastion
  master: master
  shadow: master
  mail: mail
  static-: static
credentials:
  username: '<%= @nova_user %>'
  api_key: '<%= @nova_password %>'
  auth_url: 'http://<%= @keystone_host %>:35357/v2.0'
  region_name: '<%= @region_name %>'
