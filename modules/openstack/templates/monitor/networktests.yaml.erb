# this file is managed by puppet:
# modules/openstack/templates/monitor/networktests.yaml.erb
---
- envvars:
  - SSH: <%= @ssh %>
<% @envvars.each do |key, value| -%>
    <%= key %>: <%= value %>
<% end -%>
---

- name: from cloudcontrol to cloudgw IPv4 ping
  tests:
    - cmd: timeout -k5s 10s ping4 -c1 $CLOUDGW_A >/dev/null
      stdout: ""
      retcode: 0
      stderr: ""
    - cmd: timeout -k5s 10s ping4 -c1 $CLOUDGW_B >/dev/null
      stdout: ""
      retcode: 0
      stderr: ""
    - cmd: timeout -k5s 10s ping4 -c1 $CLOUDGW_VIRT >/dev/null
      stdout: ""
      retcode: 0
      stderr: ""
    - cmd: timeout -k5s 10s ping4 -c1 $CLOUDGW_WAN >/dev/null
      stdout: ""
      retcode: 0
      stderr: ""

- name: from cloudcontrol to cloudgw IPv6 ping
  tests:
    - cmd: timeout -k5s 10s ping6 -c1 $CLOUDGW_A >/dev/null
      stdout: ""
      retcode: 0
      stderr: ""
    - cmd: timeout -k5s 10s ping6 -c1 $CLOUDGW_B >/dev/null
      stdout: ""
      retcode: 0
      stderr: ""
    - cmd: timeout -k5s 10s ping6 -c1 $CLOUDGW_VIRT >/dev/null
      stdout: ""
      retcode: 0
      stderr: ""
    - cmd: timeout -k5s 10s ping6 -c1 $CLOUDGW_WAN >/dev/null
      stdout: ""
      retcode: 0
      stderr: ""

- name: from cloudcontrol to neutron WAN IPv4 ping
  tests:
    - cmd: timeout -k5s 10s ping4 -c1 $NEUTRON_WAN >/dev/null
      stdout: ""
      retcode: 0
      stderr: ""

- name: from cloudcontrol to neutron WAN IPv6 ping
  tests:
    - cmd: timeout -k5s 10s ping6 -c1 $NEUTRON_WAN >/dev/null
      stdout: ""
      retcode: 0
      stderr: ""

# from VXLAN/dualstack
- name: from VM VXLAN/Dualstack to neutron VXLAN/Dualstack IPv6 ping
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "timeout -k5s 10s ping6 -c1 $NEUTRON_VXLAN_DUALSTACK >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/Dualstack to neutron VXLAN/Dualstack IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "timeout -k5s 10s ping4 -c1 $NEUTRON_VXLAN_DUALSTACK >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/Dualstack to VM VXLAN/IPv4-only IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "timeout -k5s 10s ping4 -c1 $VM_VXLAN_IPV4_ONLY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/Dualstack to VM VXLAN/IPv4-only-fip IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "timeout -k5s 10s ping4 -c1 $VM_VXLAN_IPV4_ONLY_FIP >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/Dualstack to VM VLAN/legacy IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "timeout -k5s 10s ping4 -c1 $VM_VLAN_LEGACY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/Dualstack to VM VLAN/legacy-fip IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "timeout -k5s 10s ping4 -c1 $VM_VLAN_LEGACY_FIP >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

# from VXLAN/IPv4-only
- name: from VM VXLAN/IPv4-only to neutron VXLAN/IPv4-only IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY "timeout -k5s 10s ping4 -c1 $NEUTRON_VXLAN_IPV4_ONLY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/IPv4-only to VM VXLAN/Dualstack IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY "timeout -k5s 10s ping4 -c1 $VM_VXLAN_DUALSTACK >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/IPv4-only to VM VXLAN/IPv4-only-fip IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY "timeout -k5s 10s ping4 -c1 $VM_VXLAN_IPV4_ONLY_FIP >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/IPv4-only to VM VLAN/legacy IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY "timeout -k5s 10s ping4 -c1 $VM_VLAN_LEGACY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/IPv4-only to VM VLAN/legacy-fip IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY "timeout -k5s 10s ping4 -c1 $VM_VLAN_LEGACY_FIP >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

# from VXLAN/IPv4-only-fip
- name: from VM VXLAN/IPv4-only-fip to neutron VXLAN/IPv4-only IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP "timeout -k5s 10s ping4 -c1 $NEUTRON_VXLAN_IPV4_ONLY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/IPv4-only-fip to VM VXLAN/Dualstack IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP "timeout -k5s 10s ping4 -c1 $VM_VXLAN_DUALSTACK >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/IPv4-only-fip to VM VXLAN/IPv4-only IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP "timeout -k5s 10s ping4 -c1 $VM_VXLAN_IPV4_ONLY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/IPv4-only-fip to VM VLAN/legacy IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP "timeout -k5s 10s ping4 -c1 $VM_VLAN_LEGACY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VXLAN/IPv4-only-fip to VM VLAN/legacy-fip IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP "timeout -k5s 10s ping4 -c1 $VM_VLAN_LEGACY_FIP >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

# from VLAN/Legacy
- name: from VM VLAN/legacy to neutron VLAN/legacy IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY "timeout -k5s 10s ping4 -c1 $NEUTRON_VLAN_LEGACY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VLAN/legacy to VM VXLAN/Dualstack IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY "timeout -k5s 10s ping4 -c1 $VM_VXLAN_DUALSTACK >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VLAN/legacy to VM VXLAN/IPv4-only IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY "timeout -k5s 10s ping4 -c1 $VM_VXLAN_IPV4_ONLY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VLAN/legacy to VM VXLAN/IPv4-only-fip IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY "timeout -k5s 10s ping4 -c1 $VM_VXLAN_IPV4_ONLY_FIP >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VLAN/legacy to VM VLAN/legacy-fip IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY "timeout -k5s 10s ping4 -c1 $VM_VLAN_LEGACY_FIP >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

# from VLAN/Legacy-fip
- name: from VM VLAN/legacy-fip to neutron VLAN/legacy IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP "timeout -k5s 10s ping4 -c1 $NEUTRON_VLAN_LEGACY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VLAN/legacy-fip to VM VXLAN/Dualstack IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP "timeout -k5s 10s ping4 -c1 $VM_VXLAN_DUALSTACK >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VLAN/legacy-fip to VM VXLAN/IPv4-only IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP "timeout -k5s 10s ping4 -c1 $VM_VXLAN_IPV4_ONLY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VLAN/legacy-fip to VM VXLAN/IPv4-only-fip IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP "timeout -k5s 10s ping4 -c1 $VM_VXLAN_IPV4_ONLY_FIP >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VM VLAN/legacy-fip to VM VLAN/legacy-fip IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP "timeout -k5s 10s ping4 -c1 $VM_VLAN_LEGACY >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

# cloudgw from within VMs
- name: from VXLAN/dualstack to cloudgw VIRT gateway IPv6 ping
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "timeout -k5s 10s ping6 -c1 $CLOUDGW_VIRT >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/dualstack to cloudgw VIRT gateway IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "timeout -k5s 10s ping4 -c1 $CLOUDGW_VIRT >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only to cloudgw VIRT gateway IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY "timeout -k5s 10s ping4 -c1 $CLOUDGW_VIRT >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only-fip to cloudgw VIRT gateway IPv4 ping
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP "timeout -k5s 10s ping4 -c1 $CLOUDGW_VIRT >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy to cloudgw VIRT gateway IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY "timeout -k5s 10s ping4 -c1 $CLOUDGW_VIRT >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy-fip to cloudgw VIRT gateway IPv4 ping
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP "timeout -k5s 10s ping4 -c1 $CLOUDGW_VIRT >/dev/null"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/dualstack contacting the internet using IPv6
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "curl -6 -s ifconfig.me | grep -q 2a02:ec80"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/dualstack contacting the internet using IPv4 uses NAT address
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "curl -4 -s ifconfig.me | grep -q $ROUTING_SOURCE_IP"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only contacting the internet uses NAT address
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY "curl -4 -s ifconfig.me | grep -q $ROUTING_SOURCE_IP"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only-fip contacting the internet uses FIP
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP "curl -4 -s ifconfig.me | grep -qv $ROUTING_SOURCE_IP"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy contacting the internet uses NAT address
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY "curl -4 -s ifconfig.me | grep -q $ROUTING_SOURCE_IP"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy-fip contacting the internet uses FIP
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP "curl -4 -s ifconfig.me | grep -qv $ROUTING_SOURCE_IP"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/dualstack contacting the wikis using IPv6
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "curl -6 -Is https://es.wikipedia.org | grep x-client-ip | grep -q $IPV6_PREFIX"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/dualstack contacting the wikis using IPv4
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "curl -4 -Is https://es.wikipedia.org | grep x-client-ip | grep -q $VM_IP_PREFIX"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only contacting the wikis without NAT address
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY "curl -4 -Is https://es.wikipedia.org | grep x-client-ip | grep -q $VM_IP_PREFIX"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only-fip contacting the wikis with FIP address
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP "curl -4 -Is https://es.wikipedia.org | grep x-client-ip | grep -q $FIP_PREFIX"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy contacting the wikis wihtout NAT address
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY "curl -4 -Is https://es.wikipedia.org | grep x-client-ip | grep -q $VM_IP_PREFIX"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy-fip contacting the wikis with FIP address
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP "curl -4 -Is https://es.wikipedia.org | grep x-client-ip | grep -q $FIP_PREFIX"
      stdout: ""
      retcode: 0
      stderr: ""

# TODO: missing IPv6 support on DNS servers
- name: from VXLAN/dualstack can contact auth DNS server, using IPv4
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "dig +short $DNS_AUTH_DOMAIN @${DNS_AUTH_IP} | grep -q ${DNS_AUTH_DOMAIN_A_RECORD}"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only can contact auth DNS server
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY "dig +short $DNS_AUTH_DOMAIN @${DNS_AUTH_IP} | grep -q ${DNS_AUTH_DOMAIN_A_RECORD}"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only-fip can contact auth DNS server
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP "dig +short $DNS_AUTH_DOMAIN @${DNS_AUTH_IP} | grep -q ${DNS_AUTH_DOMAIN_A_RECORD}"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy can contact auth DNS server
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY "dig +short $DNS_AUTH_DOMAIN @${DNS_AUTH_IP} | grep -q ${DNS_AUTH_DOMAIN_A_RECORD}"
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy-fip can contact auth DNS server
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP "dig +short $DNS_AUTH_DOMAIN @${DNS_AUTH_IP} | grep -q ${DNS_AUTH_DOMAIN_A_RECORD}"
      stdout: ""
      retcode: 0
      stderr: ""

# TODO: missing IPv6 support on DNS servers
- name: from VXLAN/dualstack can contact recursor DNS server, using IPv4
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK "dig +short wikidata.org @${DNS_RECURSOR_IP} | wc -l"
      stdout: "1"
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only can contact recursor DNS server
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY "dig +short wikidata.org @${DNS_RECURSOR_IP} | wc -l"
      stdout: "1"
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only-fip can contact recursor DNS server
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP "dig +short wikidata.org @${DNS_RECURSOR_IP} | wc -l"
      stdout: "1"
      retcode: 0
      stderr: ""

- name: from VLAN/legacy can contact recursor DNS server
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY "dig +short wikidata.org @${DNS_RECURSOR_IP} | wc -l"
      stdout: "1"
      retcode: 0
      stderr: ""

- name: from VLAN/legacy-fip can contact recursor DNS server
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP "dig +short wikidata.org @${DNS_RECURSOR_IP} | wc -l"
      stdout: "1"
      retcode: 0
      stderr: ""

# TODO: missing IPv6 support on LDAP servers
- name: from VXLAN/dualstack can contact LDAP server, using IPv4
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK 'ldapsearch -x whatever | grep -q ^"# numResponses"'
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only can contact LDAP server
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY 'ldapsearch -x whatever | grep -q ^"# numResponses"'
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only-fip can contact LDAP server
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP 'ldapsearch -x whatever | grep -q ^"# numResponses"'
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy can contact LDAP server
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY 'ldapsearch -x whatever | grep -q ^"# numResponses"'
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy-fip can contact LDAP server
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP 'ldapsearch -x whatever | grep -q ^"# numResponses"'
      stdout: ""
      retcode: 0
      stderr: ""

# TODO: missing IPv6 support on LDAP servers
- name: from VXLAN/dualstack can contact openstack APIs, using IPv4
  tests:
    - cmd: $SSH $VM_VXLAN_DUALSTACK 'curl -4 -s $KEYSTONE_URL | grep -q identity'
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only can contact openstack APIs
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY 'curl -4 -s $KEYSTONE_URL | grep -q identity'
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VXLAN/IPv4-only-fip can contact openstack APIs
  tests:
    - cmd: $SSH $VM_VXLAN_IPV4_ONLY_FIP 'curl -4 -s $KEYSTONE_URL | grep -q identity'
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy can contact openstack APIs
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY 'curl -4 -s $KEYSTONE_URL | grep -q identity'
      stdout: ""
      retcode: 0
      stderr: ""

- name: from VLAN/legacy-fip can contact openstack APIs
  tests:
    - cmd: $SSH $VM_VLAN_LEGACY_FIP 'curl -4 -s $KEYSTONE_URL | grep -q identity'
      stdout: ""
      retcode: 0
      stderr: ""

- name: puppetmasters can sync git tree
  tests:
    - cmd: $SSH $GENERAL_PUPPETMASTER 'sudo -u gitpuppet git-sync-upstream --base-dir /srv/git 2>&1 | grep -q Up-to-date'
      stdout: ""
      retcode: 0
      stderr: ""
    - cmd: $SSH $LOCAL_PUPPETMASTER 'sudo -u gitpuppet git-sync-upstream --base-dir /srv/git 2>&1 | grep -q Up-to-date'
      stdout: ""
      retcode: 0
      stderr: ""

- name: VM (using floating IP) can read dumps NFS
  tests:
    - cmd: $SSH $DUMPS_FLOATING_VM_FQDN 'file $DUMPSFILE | grep -q HTML'
      stdout: ""
      retcode: 0
      stderr: ""

- name: VM (no floating IP) can read dumps NFS
  tests:
    - cmd: $SSH $DUMPS_NO_FLOATING_VM 'file $DUMPSFILE | grep -q HTML'
      stdout: ""
      retcode: 0
      stderr: ""

<% if @region == 'eqiad1' %>
#
# eqiad1-only tests
#
- name: VM (using floating IP) can connect to wikireplicas from Toolforge
  tests:
    - cmd: $SSH $TOOLFORGE_BASTION_LOGIN 'sudo -iu tools.network-tests sql enwiki "select * from page limit 2;" | grep page_id | wc -l'
      stdout: "1"
      retcode: 0
      stderr: ""
    - cmd: $SSH $TOOLFORGE_BASTION_DEV 'sudo -iu tools.network-tests sql enwiki "select * from page limit 2;" | grep page_id | wc -l'
      stdout: "1"
      retcode: 0
      stderr: ""

- name: Toolforge webservice can be accessed from the internet
  tests:
    - cmd: curl -s -f https://network-tests.toolforge.org/files/1MB.bin --output - | file -
      stdout: "/dev/stdin: data"
      retcode: 0
      stderr: ""

- name: Toolforge bastions see herald file on project NFS
  tests:
    - cmd: timeout -k5s 60s $SSH $TOOLFORGE_BASTION_LOGIN "file /mnt/nfs/labstore-secondary-tools-project/herald"
      stdout: "/mnt/nfs/labstore-secondary-tools-project/herald: ASCII text"
      retcode: 0
      stderr: ""
    - cmd: timeout -k5s 60s $SSH $TOOLFORGE_BASTION_DEV "file /mnt/nfs/labstore-secondary-tools-project/herald"
      stdout: "/mnt/nfs/labstore-secondary-tools-project/herald: ASCII text"
      retcode: 0
      stderr: ""
<% else %>
# not including eqiad1-only tests, mostly because we only run Toolforge on eqiad1
<% end %>
