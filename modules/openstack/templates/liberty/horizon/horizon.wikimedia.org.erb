#####################################################################
### THIS FILE IS MANAGED BY PUPPET
### puppet:///openstack/horizon.wikimedia.org
#####################################################################
# vim: filetype=apache

# Horizon was the old name; send people to the new server name and switch them to https
# for good measure.
<VirtualHost *:80>
    ServerName horizon.wikimedia.org
    Redirect 301 / https://<%= @webserver_hostname %>/
</VirtualHost>
