#####################################################################
### puppet:///files/rsync/rsyncd.conf.datasets_to_peers
#####################################################################

# for rolling rsyncs between dataset hosts
[data]
# using push model for rsync, so...
uid = root
gid = root
read only = false
path = /data
hosts allow = dataset1001.wikimedia.org, dataset2.wikimedia.org
