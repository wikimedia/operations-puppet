---
client:
  hosts:
<% @hosts.each do |host| -%>
    - <%= host %>
<% end -%>
  port: 9200
  url_prefix:
  use_ssl: False
  timeout: 30
  master_only: False

logging:
  loglevel: INFO
  logfile: /var/log/elasticsearch/curator.log
  logformat: default
  blacklist: ['elasticsearch', 'urllib3']
