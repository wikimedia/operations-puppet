# editstream/server
#
# This is an Upstart job configuration file for an EditStream server.
# Instances of this job are started by the editstream/init task.
#
description "EditStream server instance"

instance $server

stop on editstream.stop

setuid editstream
setgid editstream

exec /usr/local/bin/editstream $server $redis

respawn
