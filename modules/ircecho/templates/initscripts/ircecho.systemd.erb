[Unit]
Description=ircecho
After=network.target

[Service]
User=nobody
Group=nogroup
Type=simple
EnvironmentFile=-/etc/default/ircecho
ExecStart=/usr/local/bin/ircecho --infile=${INFILE} $CHANS $NICK $SERVER $NICKSERV_PASS
Restart=always
RestartSec=10s

[Install]
WantedBy=multi-user.target
