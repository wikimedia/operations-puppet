#####################################################################
### THIS FILE IS MANAGED BY PUPPET
### puppet:///modules/contint/apache/proxy_zuul
#####################################################################
# vim: filetype=apache

ProxyPassMatch		^/zuul/status$	http://localhost:8001/status
ProxyPassMatch		^/zuul/status\.json$	http://localhost:8001/status.json
# No need for ProxyPassReverse since Zuul never redirect.

ProxyRequests		Off

# Local reverse proxy authorization override
# Most unix distribution deny proxy by default (ie /etc/apache2/mods-enabled/proxy.conf in Ubuntu)

<Proxy http://localhost:8001/*>
	Order deny,allow
	Allow from all
</Proxy>
