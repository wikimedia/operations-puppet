server {
    listen 8089;

    # Wrap the normal API but only allow GETs.
    location = / {
        proxy_pass http://127.0.0.1:5668

        limit_except GET {
            deny all;
        }
    }
}
