server {
	listen [::]:80 default_server;
	listen 80 default_server;
	server_name <%= @fqdn %>;
	error_log   /var/log/nginx/certcentral.insecure.error.log;
	access_log   off;
	location /.well-known/acme-challenge/ {
		alias /etc/certcentral/http_challenges/;
		autoindex off;
	}
}

