description "Advanced key-value store"

start on started network-services

setuid redis
setgid redis

<% unless @settings['daemonize'] == false %>expect fork<% end %>
exec /usr/bin/redis-server /etc/redis/<%= @instance_name %>.conf

respawn

# vim: set ft=upstart:
