version: 1
disable_existing_loggers: False

root:
    handlers: [logstash]
    level: INFO

handlers:
    logstash:
        class: logstash.TCPLogstashHandler
        host: <%= @logstash_host %>
        port: <%= @logstash_port %>
        version: 1
        message_type: mjolnir
