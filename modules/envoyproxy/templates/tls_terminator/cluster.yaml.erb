name: <%= @upstream_name %>
connect_timeout: <%= @connect_timeout %>s
type: strict_dns
lb_policy: round_robin
load_assignment:
  endpoints:
  - lb_endpoints:
    - endpoint:
      address:
        socket_address:
          address: <%= @fqdn %>
          port_value: <%= @upstream['upstream_port'] %>
