# This file is being maintained by Puppet.
# DO NOT EDIT

pid file = <%= rsync_pid %>
uid = nobody
gid = nobody
use chroot = <%= use_chroot %>
log format = %t %a %m %f %b
syslog facility = local3
timeout = 300
# This doesn't seem to work with rsync on Ubuntu?
# address = <%= address %>
<% if motd_file != 'UNSET' %>motd file = <%= motd_file %><% end %>
