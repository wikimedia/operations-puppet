filter {
  if [type] == "syslog" and "es" in [tags] {
    mutate { add_field => { "[@metadata][index_suffix]" => '-syslog' } }
  } else {
    mutate { add_field => { "[@metadata][index_suffix]" => '' } }
  }
}
