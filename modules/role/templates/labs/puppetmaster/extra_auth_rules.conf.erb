# Allow Horizon to ask the puppetmaster about available roles
path /resource_type
auth any
allow <%= @horizon_host %>
<%= scope.function_hiera(['puppetmaster::servers']).values.each do |puppetworker| %>
allow <%= puppetworker %>
<%end %>
