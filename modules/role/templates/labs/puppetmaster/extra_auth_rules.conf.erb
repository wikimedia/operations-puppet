# Allow Horizon to ask the puppetmaster about available roles
path /resource_type
auth any
allow <%= @horizon_host %>
<% scope.function_hiera(['puppetmaster::servers']).values.flatten(1).map { |p| p['worker'] }.sort.each do |server| -%>
allow = <%= server %>
<% end -%>
