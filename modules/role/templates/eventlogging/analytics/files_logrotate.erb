<%= @out_dir %>/*.log {
       daily
       olddir <%= @out_dir %>/archive
       notifempty
       maxage <%= @logs_max_age %>
       rotate 1000
       dateext
       compress
       delaycompress
       missingok
}
