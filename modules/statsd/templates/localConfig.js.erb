/*
  StatsD Configuration

  See <https://github.com/etsy/statsd/blob/master/exampleConfig.js>
  for a full listing of configuration options.

  This file is managed by Puppet.
*/
<%=
  @config = @settings.merge Hash['port' => @port, 'mgmt_address' => '127.0.0.1']
  @config['backends'].map! { |backend| "./backends/#{backend.split('/').last}" }
  scope.function_ordered_json([@config])
%>
