# Simple httpd configuration to show a maintenance/sorry page.
# No https configured.

<VirtualHost *:80>
    ServerName "metrics.wikimedia.org"
    DocumentRoot "<%= @docroot %>"

    <Directory />
        AllowOverride None
    </Directory>

    <Directory "<%= @docroot %>">
        Require all granted
    </Directory>
</VirtualHost>
