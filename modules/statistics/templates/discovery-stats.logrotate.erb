# This file is managed by Puppet.
# puppet:///modules/statistics/discovery-stats.logrotate
# Rotate statistics::discovery cron output logs
#
<%= @log_dir %>/*.log {
  notifempty
  maxage 10
  rotate 2
  dateext
  compress
  delaycompress
  missingok
}
