class admin::role::deployment {
    include admin::groups::deployment
    realize Admin::Group[deployment]

    include admin::groups::misc
    realize Admin::Group[wikidev]

    include admin::users::dev
    realize Admin::User[aaron]
    realize Admin::User[abaso]
    realize Admin::User[andrew]
    realize Admin::User[anomie]
    realize Admin::User[aude]
    realize Admin::User[awight]
    realize Admin::User[awjrichards]
    realize Admin::User[bd808]
    realize Admin::User[brion]
    realize Admin::User[bsitu]
    realize Admin::User[cmcmahon]
    realize Admin::User[csteipp]
    realize Admin::User[demon]
    realize Admin::User[ebernhardson]
    realize Admin::User[gdubuc]
    realize Admin::User[gjg]
    realize Admin::User[gwicke]
    realize Admin::User[halfak]
    realize Admin::User[hashar]
    realize Admin::User[kaldari]
    realize Admin::User[kartik]
    realize Admin::User[khorn]
    realize Admin::User[krinkle]
    realize Admin::User[manybubbles]
    realize Admin::User[maxsem]
    realize Admin::User[mflaschen]
    realize Admin::User[mholmquist]
    realize Admin::User[mhoover]
    realize Admin::User[milimetric]
    realize Admin::User[mlitn]
    realize Admin::User[mwalker]
    realize Admin::User[neilk]
    realize Admin::User[nikerabbit]
    realize Admin::User[pdhanda]
    realize Admin::User[pgehres]
    realize Admin::User[raindrift]
    realize Admin::User[reedy]
    realize Admin::User[rmoen]
    realize Admin::User[robla]
    realize Admin::User[spage]
    realize Admin::User[ssastry]
    realize Admin::User[tfinc]
    realize Admin::User[yurik]
}
