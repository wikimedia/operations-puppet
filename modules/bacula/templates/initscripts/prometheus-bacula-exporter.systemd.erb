[Unit]
Description=Prometheus exporter for Bacula
After=network.target

[Service]
User=bacula
Group=bacula
Type=simple
ExecStart=/usr/bin/check_bacula.py --prometheus --port=<%= @port %>
KillSignal=SIGINT
Restart=always

[Install]
WantedBy=multi-user.target
