# This file has been autogenerated by puppet. Don't edit by hand

Director {
    Name = "<%= @fqdn %>"
    DIRport = <%= @dir_port %>
    QueryFile = "/etc/bacula/scripts/query.sql"
    WorkingDirectory = "/var/lib/bacula"
    PidDirectory = "/var/run/bacula"
    Password = "<%= @bconsolepassword %>"
    Messages = Daemon
    Plugin Directory = "/usr/lib/bacula"
    Maximum Concurrent Jobs = <%= @max_dir_concur_jobs %>
    # Have the Control channel encrypted. Used for incoming console connections
    TLS Enable = yes
    TLS Require = yes
    TLS CA Certificate File = "/var/lib/puppet/ssl/certs/ca.pem"
    TLS Verify Peer = no
    TLS Certificate = "/etc/bacula/director/ssl/cert.pem"
    TLS Key = "/etc/bacula/director/ssl/server.key"
    # Heartbeat Interval = 0 #in secs
    # DirAddress =
    # DirSourceAddress =
    # Statistics Retention = 5 years
    # VerId =
    # MaximumConsoleConnections = 20
}

Messages {
    Name = Standard
    console = all, !skipped, !saved
    append = "/var/lib/bacula/log" = all, !skipped
}

Messages {
    Name = Daemon
    console = all, !skipped, !saved
    append = "/var/lib/bacula/log" = all, !skipped
}

@|"sh -c 'for f in /etc/bacula/conf.d/*.conf ; do echo @${f} ; done'"
@|"sh -c 'for f in /etc/bacula/clients.d/*.conf ; do echo @${f} ; done'"
@|"sh -c 'for f in /etc/bacula/storages.d/*.conf ; do echo @${f} ; done'"
@|"sh -c 'for f in /etc/bacula/jobs.d/*.conf ; do echo @${f} ; done'"
