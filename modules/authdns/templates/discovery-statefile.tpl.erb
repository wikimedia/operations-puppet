<% @lvs_services[@lvs]['ip'].keys.sort.each do |dc| -%>
<%= @lvs_services[@lvs]['ip'][dc] %> => {{ $data := json (getv "<%= keyspace %>/<%= @title %>/<%= dc %>") }}{{ if $data }}UP{{ else }}DOWN{{ end }}
<% end -%>
