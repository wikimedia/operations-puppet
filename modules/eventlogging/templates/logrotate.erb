# This file is managed by Puppet.
# puppet:///modules/eventlogging/logrotate

<%= @log_dir %>/*.log {
       daily
       olddir <%= @log_dir %>/archive
       notifempty
       maxage 30
       rotate 1000
       dateext
       compress
       missingok
}
