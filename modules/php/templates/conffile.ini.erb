; This file is managed by Puppet.

<%=
case @settings
when Hash
    @settings.sort.map { |k,v|
        v = v ? 1 : 0 if [TrueClass, FalseClass].include? v.class
        %Q(#{k} = "#{v}")
    }.join("\n")
when Array
    @settings.join("\n")
else
    @settings
end
%>
