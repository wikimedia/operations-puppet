#!/bin/bash
# SPDX-License-Identifier: Apache-2.0

usage() {
    echo "Usage: $(basename $0) <command>"
    echo ""
    exit
}

if [ -z "$1" ] || [ "$1" = "--help" ]; then
    usage
fi

exec "$(dirname "$0")/foreachwikiindblist" "all" "${@}"
