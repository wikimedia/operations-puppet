<%#- SPDX-License-Identifier: Apache-2.0 -%>
var conf = {
      name: 'Event Schemas',
      address: '<%= @allow_origin || "http://#{@server_name}" %>/repositories',

      visibilityOptions: {
          size: {
              use: true,
              type: 'readable' //raw, readable, both
          },
          date: {
              use: false,
              type: 'raw' //raw, moment, both
          }
      }
};
