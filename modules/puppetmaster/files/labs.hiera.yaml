:backends:
  - nuyaml
:nuyaml:
  :datadir: /etc/puppet/hiera
  :expand_path:
    - common
    - realm
:private:
  :datadir: /etc/puppet/private
:hierarchy:
  - "%{::hostname}"
  - "%{::labsproject}"
  - "%{::realm}"
  - "%{::site}"
  - private
  - common
