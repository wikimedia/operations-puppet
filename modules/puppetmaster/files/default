# Defaults for puppetmaster - sourced by /etc/init.d/puppetmaster

# Set a unicode capable locale to avoid "SERVER: invalid byte sequence in
# US-ASCII" errors when puppetmaster is started with LANG that doesn't support
# non-ASCII encoding.
# See <https://tickets.puppetlabs.com/browse/PUP-1386#comment-62325>
LANG=en_US.UTF-8

# Start puppetmaster on boot?
START=no

# Startup options.
DAEMON_OPTS=""
