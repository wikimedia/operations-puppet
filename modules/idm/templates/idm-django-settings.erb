<%#- SPDX-License-Identifier: Apache-2.0 -%>

from base_settings import *
from local_settings import * # DEBUG

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = '<%= @django_secret_key %>'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = <%- if @development -%>True<%- else -%>False<%- end -%>

ALLOWED_HOSTS = ['<%= @service_fqdn %>',]

# Database
# https://docs.djangoproject.com/en/3.2/ref/settings/#databases

<%- if @development -%>
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': '/srv/idm/db.sqlite3', # noqa
    }
}
<%- else -%>
DATABASES = {
    'default': {
        'ENGINE': '<%= @django_db_backends_mysql %>',
        'NAME': '<%= @django_mysql_db_name %>',
        'HOST': '<%= @django_mysql_db_host %>',
        'USER': '<%= @django_mysql_db_user %>',
        'PASSWORD': '<%= @django_mysql_db_password %>',
    }
}
<%- end -%>

LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {
        'django': {
            'level': 'INFO',
            'class': 'logging.FileHandler',
            'filename': '<%= @idm_log_dir %>/django.log',
        },
        'bitu': {
            'level': 'INFO',
            'class': 'logging.FileHandler',
            'filename': '<%= @idm_log_dir %>/idm.log',
        },
    },
    'loggers': {
        'django': {
            'handlers': ['django'],
            'level': 'INFO',
            'propagate': True,
        },
        'bitu': {
            'handlers': ['bitu'],
            'level': 'INFO',
            'propagate': True,
        },
    },
}

RQ_QUEUES = {
    'default': {
        'HOST': 'localhost',
        'PORT': 6379,
        'DB': 0,
        'DEFAULT_TIMEOUT': 360,
    },
    'notification': {
        'HOST': 'localhost',
        'PORT': 6379,
        'DB': 0,
        'DEFAULT_TIMEOUT': 360,
    },
}

