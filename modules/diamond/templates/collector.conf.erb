# <%= @title %> Diamond Collector configuration
# This file is managed by Puppet.
<%=
    Hash["enabled" => "true"].update(@settings).sort.map { |k,v|
        v = v.is_a?(Array) ? v.join(',') : ( v == :undef ? '' : v )
        "#{k} = #{v}"
    }.join("\n")
%>
