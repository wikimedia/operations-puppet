<%#- SPDX-License-Identifier: Apache-2.0 -%>
# Arguments used by SysV init script, systemd service file uses only EXTRA_ARGS
#PORT=<%= @port %>
#LOGS=<%= @logs.join(',') %>

EXTRA_ARGS="-disable_fsnotify -logs <%= @logs.join(',') %> -port <%= @port %> <%= @additional_args %>"
