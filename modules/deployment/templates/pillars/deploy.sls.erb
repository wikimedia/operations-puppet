<%
require 'yaml'

puts({'repo_locations' => deployment_repo_locations.sort}.to_yaml)
puts({'repo_grains' => deployment_repo_grains.sort}.to_yaml)
puts({'repo_checkout_submodules' => deployment_repo_checkout_submodules.sort}.to_yaml)
puts({'repo_urls' => deployment_repo_urls.sort}.to_yaml)
puts({'repo_regex' => deployment_repo_regex.sort}.to_yaml)
puts({'repo_checkout_module_calls' => deployment_repo_checkout_module_calls.sort}.to_yaml)
puts({'repo_dependencies' => deployment_repo_dependencies.sort}.to_yaml)
puts({'deploy_redis' => {'host' => deployment_deploy_redis['host'], 'port' => deployment_deploy_redis['port'], 'db' => deployment_deploy_redis['db']}}.to_yaml)
puts({'deploy_servers' => deployment_servers.sort}.to_yaml)
%>
