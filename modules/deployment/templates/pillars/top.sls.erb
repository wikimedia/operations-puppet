base:
  'deployment_server:true':
    - match: grain
    - deployment.repo_config
    - deployment.deployment_config
  'deployment_target:*':
    - match: grain
    - deployment.repo_config
    - deployment.deployment_config
