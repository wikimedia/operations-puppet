HOSTS:
  centos-7-x64:
    default_apply_opts:
      order: random
      strict_variables:
    platform: el-7-x86_64
    hypervisor : openstack
    flavor: m1.small
    image: centos-7-latest
    user: centos
CONFIG:
  type: foss
  log_level: debug
  openstack_network: default
