HOSTS:
  debian-6-x64:
    default_apply_opts:
      order: random
      strict_variables:
    platform: debian-6-amd64
    hypervisor : openstack
    flavor: m1.small
    image: debian-6-latest
    user: debian
CONFIG:
  type: foss
  log_level: debug
  openstack_network: default
