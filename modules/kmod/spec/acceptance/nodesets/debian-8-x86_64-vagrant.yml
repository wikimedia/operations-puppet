HOSTS:
  debian-8-x64:
    default_apply_opts:
      order: random
      strict_variables:
    platform: debian-8-amd64
    hypervisor : vagrant
    box : camptocamp/debian-8-amd64
CONFIG:
  type: foss
  log_level: debug
