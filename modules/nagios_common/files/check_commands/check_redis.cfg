define command{
       command_name     check_redis_replication
       command_line     $USER1$/check_redis -H $HOSTADDRESS$ -C /etc/icinga/.redis_secret -p $ARG1$ -r $ARG2$,$ARG3$
}

define command{
       command_name     check_redis
       command_line     $USER1$/check_redis -C /etc/icinga/.redis_secret -H $HOSTADDRESS$ -p $ARG1$
}

define command{
       command_name     check_redis_replication_password
       command_line     $USER1$/check_redis -H $HOSTADDRESS$ -p $ARG1$ -r $ARG2$,$ARG3$ -x $ARG4$
}

define command{
       command_name     check_redis_password
       command_line     $USER1$/check_redis -H $HOSTADDRESS$ -p $ARG1$ -x $ARG2$
}
