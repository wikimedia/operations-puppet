#####################################################################
### THIS FILE IS MANAGED BY PUPPET
### puppet:///templates/misc/cxserver.default.erb
#####################################################################

# Checkout of mediawiki/services/cxserver
<% if @cxserver_base_path then %>
CXSERVER_BASE_PATH=<%= @cxserver_base_path %>
<% else %>
# Override in puppet using cxserver_base_path
#CXSERVER_BASE_PATH
<% end %>

# Checkout of mediawiki/services/cxserver/deploy
<% if @cxserver_node_path then %>
NODE_PATH=<%= @cxserver_node_path %>
<% else %>
# Override in puppet using cxserver_node_path
#NODE_PATH=
<% end %>

<% if @cxserver_log_dir then %>
CXSERVER_LOG_DIR=<%= @cxserver_log_dir %>
<% else %>
# Override in puppet using cxserver_log_dir
#CXSERVER_LOG_DIR=
<% end %>
