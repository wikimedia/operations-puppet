// Varnish VCL include file for maps backends

include "errorpage.inc.vcl";

sub vcl_recv {
	call recv_purge;
	return (<%= @hash_lookup %>);
}

<% if @varnish_version4 -%>
sub vcl_backend_error {
<% else -%>
sub vcl_error {
<% end -%>
	call errorpage;
	return (deliver);
}
