# Note: This file is managed by Puppet.

description "webstatscollector collector process"

start on filesystem
stop on runlevel 5

respawn
respawn limit 10 5
umask 022

setuid nobody
setgid nogroup

chdir <%= @webstats_temp_directory %>

exec /usr/local/bin/collector