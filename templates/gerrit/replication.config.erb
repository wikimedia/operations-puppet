####################################
# This file is generated by puppet #
####################################
<%
# replication is a hash which maps remote "names" to their URLs
# and other settings

if replication != "" then
  replication.each { |remote,settings|
    %>[remote "<%= remote %>"]
<%
    settings.each { |option,value|
      %>  <%= option %> = <%= value %>
<%
    }
  }
end
%>
