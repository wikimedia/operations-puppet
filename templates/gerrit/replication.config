##################################
# This file is managed by puppet #
##################################
<%
# replication is a hash which maps remote "names" to their URL

replication.each { |remote,destination|
%>
[remote "<%= name %>"]
  url = <%= destination %>
  threads = 4
<%
}
