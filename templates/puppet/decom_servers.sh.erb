#!/bin/bash
for srv in $(cut -d'"' -f 2 -s <%= scope.lookupvar('puppetmaster::config::gitdir') %>/operations/puppet/manifests/decomissioning.pp)
	do puppetstoredconfigclean.rb $srv.wikimedia.org $srv.esams.wikimedia.org $srv.pmtpa.wmnet $srv.eqiad.wmnet
done

